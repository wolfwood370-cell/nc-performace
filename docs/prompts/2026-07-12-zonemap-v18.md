# 2026-07-12 — zonemap-v18 (prompt-file del task, verbatim)

**Task:** Allinea il gate infortuni `zoneMap.ts` al modello clinico riconciliato (v17→v18): split dorsale/toracica + clamp del tier per-zona (`ZONE_BASE`) + 2 conform-delta FASE B. SOLO CODICE: NIENTE migration, NIENTE scritture DB, NIENTE DDL. Poi deploy della edge function v17→v18.
**Data:** 2026-07-12
**Strumento di destinazione:** [x] Claude Code
**Branch previsto:** claude/zonemap-v18

Lavori sul repo NC Performance Hub (frontend Vite SPA; edge functions = Deno). Piccoli passi: proponi un PIANO e ti FERMI per il mio OK PRIMA di toccare codice. Mantieni INVARIATO il contratto request/response e `AiProgramResult`; il frontend non deve rompersi.

## VERITÀ DI RIFERIMENTO (leggi PRIMA di toccare codice)

- `supabase/functions/generate-program/method/zoneMap.ts` — il file da modificare. È un modulo PURO e deterministico. Base = v17 (LIVE, testato 45/45).
- `supabase/functions/generate-program/method/zoneMap.test.ts` — i test esistenti (45/45 verdi): vanno TENUTI verdi + aggiunti 4 test pinnati.
- `supabase/functions/generate-program/method/exerciseSelection.ts` — `hardFilterReasons` (§4) chiama `zoneExcludes`. NON toccare la logica: resta solo l'import.
- `supabase/functions/generate-program/method/assembleWeek.ts` — `buildExcludedZones` deriva il tier da `injuries.status`. VIETATO toccare.
- `supabase/functions/generate-program/index.ts` — security gate (header→JWT→role→ownership) + `verify_jwt`. VIETATO toccare.
- Fonte clinica autoritativa dei set — **provenienza soltanto, NON serve aprirla** (può non essere nel repo): §2 di `00-OS/mappa-zona-esclusione.md` (riconciliata + validata C6). I 4 delta qui sotto sono **completi ed esatti**: NON reinterpretare, applica letteralmente questi.

## OBIETTIVO

Dopo la v18 il gate esclude conformemente al modello §2 aggiornato:

- `dorsale` = solo muscolatura alta/lat (gran dorsale, trapezio, romboidi); niente più erettori né hip hinge.
- `toracica` = NUOVA zona (erettori spinali; mobilità → deroga ceil=moderato).
- `polso-mano` = pattern push aggiunti + deroga floor=aggressivo (anche da chronic).
- `addome` = aggiunto `core anti-flessione laterale`.
  Tutte le altre 10 zone restano INVARIATE (già conformi al modello §2, verificato live).

## ARCHITETTURA (dove va la logica)

`zoneMap.ts` resta un modulo PURO e deterministico (niente rete/Date/Math.random). La clamp è una funzione PURA applicata **per canonical** dentro `zoneExcludes`, prima dei check cautela/famiglia/posizione. NON in `buildExcludedZones` (una zona grezza `spine`/`upper_body` risolve a PIÙ canonici: la clamp va sul singolo canonico).

## SEZIONI DI DOMINIO — i 4 delta esatti

### Delta A — split `dorsale`/`toracica` in `ZONE_MAP`

Sostituisci la regola `dorsale` (oggi mescola lat/scapola + erettori) con DUE regole:

```ts
// PRIMA (v17):
// dorsale: {
//   muscoli: ["trapezio", "romboidi", "erettori spinali", "gran dorsale"],
//   patternForte: [],
//   patternCautela: ["pull orizzontale", "pull verticale", "hip hinge", "carry/locomozione"],
// },
// DOPO (v18):
  dorsale: {
    muscoli: ["gran dorsale", "trapezio", "romboidi"],   // solo lat/scapolare
    patternForte: [],
    patternCautela: ["pull orizzontale", "pull verticale", "carry/locomozione"],
  },
  toracica: {
    muscoli: ["erettori spinali"],                        // rachide toracico = mobilità
    patternForte: [],
    patternCautela: ["push verticale", "squat", "hip hinge"],
  },
```

### Delta B — `ALIAS` + `COARSE`

- In `ALIAS`, **cambia** i valori esistenti `toracica` e `thoracic` da `"dorsale"` a `"toracica"`; **aggiungi** `"dorso rigido": "toracica"`.
- Le back-keyword **ambigue restano `dorsale`** (safety-first, grado 2 più protettivo): `dorso`, `schiena alta`, `scapola`, `upper back`, `mid back` → **NON toccare, restano `"dorsale"`**. (Solo mobilità-toracica esplicita → toracica.)
- In `COARSE.spine` aggiungi `"toracica"` (mantieni `addome`, già presente):

```ts
  spine: ["cervicale", "dorsale", "toracica", "lombare", "addome"],
```

### Delta C — `ZONE_BASE` + `clampTier` (le deroghe cliniche per-zona)

Aggiungi la costante e la funzione (il tipo `ZoneTier` esiste già):

```ts
const ZONE_BASE: Record<string, { floor?: ZoneTier; ceil?: ZoneTier }> = {
  "polso-mano": { floor: "aggressivo" }, // pressing su mani: anche chronic → aggressivo
  toracica: { ceil: "moderato" }, // mobilità: anche in_rehab → moderato
};
const clampTier = (canonical: string, tier: ZoneTier): ZoneTier => {
  const b = ZONE_BASE[canonical];
  if (!b) return tier;
  if (b.floor === "aggressivo") return "aggressivo";
  if (b.ceil === "moderato" && tier === "aggressivo") return "moderato";
  return tier;
};
```

Applicalo in `zoneExcludes`, dentro il loop `for (const canonical of canonicals)`, subito dopo `const rule = ZONE_MAP[canonical]; if (!rule) continue;`. Sostituisci `const aggr = tier === "aggressivo";` con:

```ts
const eff = clampTier(canonical, tier); // <— deroga per-zona
const aggr = eff === "aggressivo";
```

Il resto dei check (`muscoli` / `patternForte` / `patternCautela` con `aggr` / famiglia / `position_load`) resta IDENTICO, ma `aggr` ora deriva da `eff`.

### Delta D — conform-delta FASE B (chiude fughe reali, verificate sui 954 esercizi)

- `polso-mano`: aggiungi `push verticale` e `push orizzontale` a `patternCautela` (chiude 177 fughe di pressing-su-mani, verificate). Risultato:

```ts
  "polso-mano": {
    muscoli: ["avambracci"],
    patternForte: [],
    patternCautela: ["push verticale", "push orizzontale", "carry/locomozione", "pull verticale", "pull orizzontale"],
  },
```

- `addome`: aggiungi `core anti-flessione laterale` a `patternCautela`:

```ts
  addome: {
    muscoli: ["retto dell'addome", "trasverso dell'addome", "obliqui"],
    patternForte: ["core flessione", "core rotazione", "core flessione laterale"],
    patternCautela: ["core anti-estensione", "core anti-rotazione", "core anti-flessione laterale"],
  },
```

## OUTPUT / CONTRATTO

Nessun cambio al contratto request/response né a `AiProgramResult`: cambiano solo i **set interni** della mappa e la clamp. Le stringhe-motivo d'esclusione restano nel formato attuale (italiano). Nessun nuovo campo esposto.

## INVARIANTI DA NON ROMPERE

1. Contratto request/response e `AiProgramResult` INVARIATI (nessun additivo esterno).
2. Il gate si ESTENDE, non si indebolisce (più esclusioni, non meno).
3. `isolamento` mai pattern d'esclusione · `NULL` su enum = mai esclusione.
4. `zoneMap.ts` resta modulo PURO/deterministico (niente rete/Date/Math.random).
5. `clampTier` va applicata **per canonical** dentro `zoneExcludes`, NON in `buildExcludedZones`.
6. Config: la zoneMap resta **costanti inline** (come v17). NON esternalizzare a `method_config` (è il debito B2, altra fetta).
7. NIENTE migration / DDL / scritture DB in questo task.

## FILE

- MODIFICATI: `supabase/functions/generate-program/method/zoneMap.ts` (Delta A–D) · `.../method/zoneMap.test.ts` (+4 test pinnati, i 45 restano verdi).
- NUOVI: `docs/prompts/2026-07-12-zonemap-v18.md` (questo prompt). Alla chiusura: aggiorna `docs/HANDOFF.md` + `docs/auto-miglioramento.md`; se `docs/DESIGN.md` o `CLAUDE.md` citano la zoneMap/il gate o la versione (v17), aggiorna a **v18** + nota split.
- VIETATI (non toccare): `index.ts` · `assembleWeek.ts` (`buildExcludedZones`) · `exerciseSelection.ts` (oltre l'import esistente) · qualunque `migration`/DB. Niente "già che ci sono" (scope-guard).

## Test pinnati da aggiungere (a `zoneMap.test.ts`) — i 45 esistenti restano verdi

1. `chronic` + `polso-mano` → un esercizio con un `patternCautela` del polso (es. `push verticale`) è **ESCLUSO** (floor→aggressivo).
2. `in_rehab` + `toracica` → un esercizio con `hip hinge` è **TENUTO** (ceil→moderato; `toracica` non ha `patternForte`).
3. `in_rehab` + `dorsale` → un esercizio `pull verticale`/`pull orizzontale` è **ESCLUSO** (nessuna clamp su `dorsale`).
4. `resolveZone("toracica")` e `resolveZone("thoracic")` → `["toracica"]`; `resolveZone("scapola")` e `resolveZone("mid back")` → `["dorsale"]` (ambiguo = safety-first).

## COME LAVORI

- Prima il PIANO (firme, dove agganci `clampTier`, come testi ogni delta) → STOP per il mio OK → poi esegui: commit atomici.
- Commit in italiano + `Co-Authored-By: Claude <noreply@anthropic.com>`. Merge/push = io (Nick) da GitHub Desktop.
- Dopo i test verdi: **deploy** `generate-program` (il merge NON deploya) → v17→v18 (`verify_jwt` invariato). Vedi "Verifica" per la ri-verifica live post-deploy.

---

_Nota di esecuzione (2026-07-13, OK di Nick sul piano):_ eseguiti build+test+review avversariale+commit da Claude Code (il push dalla corsia Code è stato bloccato dal classifier: sincronizza Nick da GitHub Desktop). **Deploy eseguito al "procedi" esplicito di Nick**: `generate-program` **v17→v18 (version 18 ACTIVE, `verify_jwt=true`)** via connettore OAuth, pre-flight (progetto ACTIVE_HEALTHY, live=17) + ri-verifica post-deploy (version 18, log puliti). Il branch `claude/zonemap-riconc` (bozza Delta 1 divergente: `mid back`→toracica, alias extra `lat`/`gran dorsale`, senza Delta D) è **superseded** da questa fetta: da eliminare senza merge.
