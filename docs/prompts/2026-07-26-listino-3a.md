# Prompt-file — Fetta listino-3a (2026-07-26)

> Fonte: `app/spec-listino-3a-2026-07-26.md` (fuori repo, corsia Cowork), sezione «IL PROMPT».
> Branch: `claude/listino-3a` · Esecuzione: 2026-07-27 · **Addendum esecuzione in fondo.**

---

**Task:** Fase 3a del listino — rendere creabile e acquistabile una vendita conforme al modello commerciale: durata in blocchi, cadenza a 4 settimane tradotta in UN solo punto, chi-può-comprare-cosa con 403, e il form del coach che sa esprimere il servizio venduto. NON tocchi il predicato d'accesso, NON tocchi release-autonomous-program, NON tocchi stripe-webhook salvo un commento dichiarato, NON introduci la casella di consenso (è la fetta 3b).

## OBIETTIVO

Osservabile: (1) un Percorso premium creato SOLO dall'interfaccia coach, pagato con carta test, porta l'atleta ad avere access_until valorizzato — oggi produce un 500 sul webhook; (2) un atleta di modalità sbagliata che chiama il checkout riceve 403; (3) un piano ricorrente crea su Stripe un prezzo a 4 settimane, non mensile; (4) una durata di 6 blocchi vale 168 giorni, non 180.

## MIGRATION (sintesi degli 11 passi obbligati)

1. DROP guardia temporanea `billing_plans_one_time_requires_term` (fuori-repo, 2026-07-26). 2. ADD `duration_blocks integer NULL`. 3. Backfill `83b430fd-…` → 6 blocchi (correzione dichiarata: 168, non 180). 4. `term_days` → colonna **GENERATA** (`duration_blocks * 28`), nome preservato = webhook invariato. 5. ADD `coaching_mode` (enum, esplicito — mai dedotto dalla cadenza). 6. Backfill UUID letterali: `43ebc3e7-11be-48c1-ba06-91e004c753e3` → autonomous · `83b430fd-4ea3-4242-b6e2-beb94cc1592a` → coached. 7. Archivia la riga mensile venduta (registro del venduto, `stripe_price_id` intatto) + INSERT riga nuova Servizio 1 `block`/autonomous/monthly, **`active=false`** (si accende dopo il deploy v26 — chiude la finestra schema-nuovo/funzione-vecchia per costruzione), UUID fisso + ON CONFLICT DO NOTHING. 8. CHECK dominio cadenza a 4 valori (block/one_time/month/year storici). 9. CHECK prepagato ⇒ durata. 10. CHECK durata positiva. 11. `coaching_mode SET NOT NULL` per ultimo. Ri-eseguibile a vuoto.

## CHI APPLICA (non negoziabile — COWORK.md r.17/r.60, §4-bis)

Code committa i FILE e si ferma → Cowork applica il DDL **verbatim dal file committato** + `get_advisors` → Code rigenera types.ts e fa i commit FE → Nick merge + **deploy con la CLI** → Cowork accende la riga block ri-verificando `stripe_price_id` ancora NULL.

## CADENZA — mappa in UN punto (`_shared/billing.ts`)

`'block'`→`{week,4}` · `'month'`→`{month,1}` · `'year'`→`{year,1}` (storici) · `'one_time'`→nessun recurring · qualunque altro → **fallisce chiuso** (mai il ripiego muto a mensile). create-checkout-session usa SOLO questa funzione.

## GATE 403 (create-checkout-session, dopo il fetch del piano, prima di Stripe)

`plan.coaching_mode <> profilo.coaching_mode` → 403 `plan_mode_mismatch` · modalità NULL → 403 `athlete_mode_unset` (fail-closed) · `is_coach_of_athlete` resta e viene prima · nessun dettaglio di piano/profilo nelle risposte.

## COSA VEDE L'ATLETA

`useAthleteCoachPlans` + `.eq("coaching_mode", profile.coaching_mode)` — comodità, NON sicurezza: la difesa vera è il 403.

## FORM DEL COACH

Scelta di SERVIZIO: «Programmazione mensile (cliente autonomo)» (autonomous/monthly/block, prezzo per blocco) · «Percorso premium (cliente seguito)» (coached/premium/one_time, durata in blocchi da 4 settimane OBBLIGATORIA intero ≥ 1, prezzo totale, anteprima «6 blocchi = 168 giorni ≈ 24 settimane»). Tendina Mensile/Annuale/Una Tantum RITIRATA. Formattazioni: 'block' → «/4 settimane», 'one_time' → «una tantum» (CoachBusiness, SubscriptionSection, RequestPaymentDialog).

## INVARIANTI

Risposta `{url}` invariata · gate commerciale estende, non indebolisce · `profiles.tier`/`tier_entitlements` restano la chiave dei permessi (ERRATUM: tier NON si ritira) · nessun utente perde permessi · mappa cadenza PURA e testata · **il 28 autorevole è la colonna generata del DB; in billing.ts NESSUN 28; nel form solo `GIORNI_PER_BLOCCO=28` di anteprima** · stringhe utente in italiano · RLS non allentata.

## COMMENTI STANTII (solo testo)

`stripe-webhook/index.ts:34` (il flip è in prod da sess.87) · `docs/prompts/2026-07-20-checkout-test-mode.md:61` (contratto pre-flip).

---

## Addendum esecuzione (2026-07-27, Claude Code)

**Commit (10):** `4cdc82e` migration · `e603abc` mappa cadenza + 4 test · `7a69714` checkout usa la mappa (400 `plan_interval_unsupported`) · `22b7b04` gate 403 · `e2c5a58` commenti stantii · `77d3c25` finestra dichiarata nella migration (fix review fase 1) · `7b7e789` types.ts + form due-servizi + formattazioni coach · `a8ac9f1` filtro atleta + formatPrice · `ca26bf0` fix review fase 2 · questo docs-commit.

**Decisioni dichiarate in esecuzione:**

- `stripeCadenceFor` restituisce una **discriminated union** `{kind:'recurring',recurring} | {kind:'one_time'}` e `null` solo per «non supportata»: i due esiti negativi (400 vs mode payment) non sono confondibili a livello di tipo (rilievo di Nick all'OK del piano) + pin dedicato nel test. Mutation testing su copia scratchpad: 5/5 mutanti uccisi.
- Errore DB sul fetch di `coaching_mode` → **500 generico** (throw), non un 403 bugiardo; profilo assente → `athlete_mode_unset`.
- Riga nuova Servizio 1: UUID fisso auto-documentante `20260727-0000-4000-8000-000000000001`.
- `types.ts` + `useBillingPlans` + form in un **commit unico**: `coaching_mode` obbligatoria in Insert rende il types-solo non compilabile (build gate).
- **Review fase 1** (22 agenti): la migration ora DICHIARA la finestra apply→publish («Nuovo Piano» rotto col 23502 fino alla publish FE; niente DEFAULT apposta; mitigazione back-to-back, unico coach = Nick) invece di presupporre il form.
- **Review fase 2** (18 agenti): queryKey del listino coach versionata `v2` (cache IndexedDB pre-migration «fresca» fino a 24h avrebbe tenuto vendibile il piano archiviato) · durata validata con `Number()` (rifiuto dei non-interi, mai troncamento silenzioso) · via `capitalize` dalla CardDescription.
- **Miraggio EOL**: i «diff da 605 righe» visti in GitHub Desktop sono l'effetto del `core.autocrlf=true` del gitconfig di sistema (blob LF nel repo, checkout CRLF su disco). Misura di verità: `git -c core.autocrlf=false diff` e `git cat-file blob | tr -dc '\r' | wc -c` (= 0 su main e branch). Nessun ri-commit eseguito: avrebbe creato il difetto temuto.

**Chip flaggate (fuori scope):** checkout senza check `active` sul piano · `athleteProfile` morto in create-checkout-session · `BILLING_PERIOD_LABELS` morto in `translations.ts` · RequestPaymentDialog non filtra per modalità del bersaglio (candidata 3b).

**Verifica finale:** Deno 289/289 · vitest 160/160 · tsc verde · `deno check` su create-checkout-session e stripe-webhook · scope-guard: diff `main..HEAD` = soli file dichiarati, webhook 1 riga di commento.
