# AUTO-MIGLIORAMENTO — diario di processo (nc-performance-hub)

> **Cos'è.** Le lezioni su **COME lavorare** su questo repo (non sul contenuto-metodo: quello vive in `.claude/methodology/`). Serve a non ripetere gli errori e a standardizzare ciò che funziona. Vale per **Claude Code** e per **Cowork**.
> **Quando si usa.** Si **legge all'INIZIO** di ogni sessione (dopo `CLAUDE.md`/`COWORK.md` + `00-CORE.md`) e si **aggiorna alla FINE**, con una RETRO subito prima dell'handoff.
> **Regola d'oro.** Concreto e **falsificabile**, niente auto-elogio: se una voce non cambierebbe un comportamento, non si scrive.

## Come si usa (rituale)

- **INIZIO:** leggi «Cosa funziona» + «Fragilità & errori ricorrenti» + le ultime 1-2 voci del Log → entri in sessione sapendo dove si inciampa.
- **FINE (prima dell'handoff):** RETRO in 3 caselle + una voce datata nel Log:
  1. **Funzionato** (da tenere) · 2. **Subottimale** (+ il fix, anche sui propri passi falsi) · 3. **Migliorie azionabili** (dove le persisti).
- **Promozione:** una lezione che si ripete → spostala da «Log» a «Cosa funziona»/«Fragilità». Il Log è append-only.

---

## DA NON FARE — paletti (controlla prima di agire)

> Divieti netti e **falsificabili**. Nati da incidenti reali o da regole non negoziabili di `CLAUDE.md`/`COWORK.md`.

**Git & repo**

- **NON** `push` mai: si opera in `.claude/worktrees/<slug>` su branch `claude/<slug>`; sincronizza **Nick** via GitHub Desktop (`CLAUDE.md` legge #8).
- **NON** committare con il **build-gate rosso**: `npx tsc --noEmit -p tsconfig.app.json` deve essere verde (legge #3).
- **NON** fare git di **scrittura dalla sandbox Cowork** su repo montato: lascia `.git/index.lock` non rimovibile e **blocca il Pull di GitHub Desktop**. Cowork = git **read-only** (`COWORK.md` legge #1).
- **(Cowork) NON** scrivere file nel working tree (nemmeno `HANDOFF`/migrazioni): sono untracked → **stash-loop** al primo switch. Li committa **Code** (`COWORK.md` legge #2).

**DB & security**

- **(Code) NON** applicare migration/DDL sul DB: niente MCP Supabase in Code → proponi il **FILE** `supabase/migrations/*`. Il DB lo opera **Cowork** col benestare di Nick (`CLAUDE.md` legge #11).
- **NON** usare l'**hand-patch `types.ts`** storico (droppare/ripristinare `appointments`): con il DB di proprietà è **obsoleto** → rigenera via `supabase gen types typescript --linked` (legge #7).
- **NON** toccare RLS/`SECURITY DEFINER`/advisor "perché posso": ownership condivisa → **STOP & ASK** (`CLAUDE.md §5`).
- **NON** proporre un distruttivo (drop, `stash drop`, `rm`) senza averne ispezionato il contenuto prima (`COWORK.md` legge #3).

**Codice & scope**

- **NON** "while you're here": un commit = un intervento logico; flagga il resto, non mescolare scope (legge #2/#4).
- **NON** mescolare token Coach/Athlete: `src/components/coach/**` non usa `.theme-athlete` e viceversa (`CLAUDE.md §2`).
- **NON** mettere hook dopo un `return` early: tutti gli hook prima (legge #6; `00-CORE §8`).
- **NON** fidarsi del "success" di uno strumento (Lovable, connettore, form-builder): **verifica nel diff reale / nel browser** prima di dire fatto.

**Segreti & confini**

- **NON** stampare/committare `.env`, secrets, chiavi Stripe/Google: sono di **Nick** (`COWORK.md` legge #4).
- **NON** committare `console.log`: usa `src/lib/logger.ts` (legge #10).

## Cosa funziona (tenere / standardizzare)

1. **Misura prima di agire** (`wc -l`/`Grep` mirato): mai indovinare la struttura di un file.
2. **Commit atomici + verifica-commit immediata** (`git log -1` + `git status`): lo stato è sempre noto.
3. **Build-gate deterministico** (hook `tsc --noEmit` PostToolUse): "il modello non può dimenticare".
4. **Worktree isolato** per branch: il `main` resta pulito, il merge lo fa Nick.
5. **Verifica-nel-diff, non nel "success"**: dopo un tool esterno, `git diff` reale (o browser) prima di fidarsi.
6. **STOP & ASK sui casi §5** (data loss / RLS / breaking API / business): una decisione alla volta, dichiarata in 1 riga.
7. **Divisione DB/codice**: Cowork opera il DB (connettore + advisors), Code scrive i file di migrazione → niente "lethal trifecta" in mano all'esecutore-codice.

## Fragilità & errori ricorrenti (+ guardia)

1. **Sandbox `.git/index.lock`** (git write da Cowork) → blocca GHD. Guardia: Cowork solo git read-only.
2. **"Success" ingannevole** degli strumenti AI (Lovable/form-builder/connettore): il messaggio non prova il risultato → verifica reale.
3. **Sync Lovable silenzioso** (se Lovable è nel giro): l'editor può non riflettere i commit esterni → verifica l'allineamento prima di editarci; mai due strumenti sugli stessi file insieme.
4. **types.ts**: dopo un cambio schema, dimenticare `supabase gen types --linked` → tipi stale. Guardia: rigenera + build-gate.

---

## Log delle retrospettive (append-only)

### 2026-07-04 — metodo v2: allineamento dei file-guida al DB di proprietà

**Funzionato.** Il pacchetto "metodo v2" ha allineato `CLAUDE.md`/`COWORK.md`/`03`/`04` alla realtà (Supabase proprio + 5 attori), eseguendo il piano già scritto in `docs/DB_MIGRATION.md §8/App.A` invece di re-deciderlo → chiusa la **D5** (security ownership) col modello a ownership condivisa.
**Subottimale (+fix).** I file-guida erano rimasti indietro rispetto allo stack reale (Lovable-ism sparsi). **Fix:** questo file + il rituale RETRO a ogni sessione, così lo scarto file-guida↔realtà si nota subito.
**Migliorie azionabili.** (1) Leggere questo file a inizio sessione (aggiunto a `CLAUDE.md §4/§6`). (2) A ogni cambio di stack/ownership, `grep` dei termini obsoleti (es. "Lovable Cloud", "Lovable Security Agent") come check di regressione.

### 2026-07-04 — applicazione del pacchetto metodo v2 (Claude Code, branch `claude/metodo-v2`)

**Funzionato.** Spec chirurgiche con acceptance-grep falsificabili: 8 commit atomici applicati senza ambiguità; la review avversariale multi-agente post-sweep ha trovato 2 residui reali (un qualificatore difforme dalla legge #11 in `03 §0`; uno "Stitch" in un report di esempio di `05` r.416) poi corretti.
**Subottimale (+fix).** (1) Gli acceptance-grep "=0" del pacchetto non prevedevano che le bozze stesse citassero i termini obsoleti per negarli (`CLAUDE.md` legge #11, `COWORK.md` legge 5, l'esempio di grep qui sopra) né le righe diff storiche di `DB_MIGRATION.md App.A`: eccezioni documentate a mano. **Fix:** nei prossimi pacchetti, dichiarare le eccezioni attese direttamente nell'acceptance. (2) Micro-tensione interna al pacchetto: `CLAUDE.md` legge #11 dice "niente MCP Supabase in Code" ma la toolmap `COWORK.md §4` concede a Code le letture MCP (e `docs/CLAUDE_CODE_SETUP.md §1` configura l'MCP read-only in Code) → da riconciliare con Nick alla prossima revisione dei file-guida.
**Migliorie azionabili.** Il check di regressione (2) della voce precedente è stato eseguito qui e funziona: tenerlo come passo standard dopo ogni sweep di rename/ownership.

### 2026-07-05 — invito nativo + email automatica (branch `claude/invito-nativo-auto-email`)

**Funzionato.** Il prompt-file con "verità di riferimento" (file + righe esatte da cambiare) ha azzerato l'esplorazione a vuoto; la version canonica della migration è arrivata da Cowork/Nick e il file mirror rispecchia `schema_migrations` (disciplina §4-bis rispettata); la review avversariale post-implementazione ha trovato problemi reali (toast in inglese su percorsi non mappati; HTML injection del nome nell'email, amplificata dal nuovo mittente verificato) corretti prima della chiusura. Il classificatore ha bloccato la lettura MCP dalla corsia Code: la legge #11 regge anche quando "sarebbe comodo".
**Subottimale (+fix).** Il flusso nativo promosso a primario NON ha un percorso di reinvio: dopo un 502 Resend / email in spam / link scaduto, il retry risponde `alreadyLinked` senza inviare nulla (l'utente auth nasce già a `generateLink`) e nemmeno il fallback manuale recupera. **Fix di processo:** quando si promuove un flusso a "primario", chiedersi sempre «e il retry/reinvio?» prima del rollout — flaggato come task dedicato, non mescolato allo scope.
**Migliorie azionabili.** (1) Task flaggato: reinvio invito + hardening ramo attach + paginazione `listUsers`. (2) Al merge, delta Cowork su `HANDOFF §2` (la riga "edge fn non usata dalla UI" diventa falsa) e `PRODUCT_SPEC` (~r.262). (3) Task flaggato: ritiro del flusso manuale quando il nativo è provato.

### 2026-07-05 — motore-metodo M1: moduli deterministici in `generate-program/method/` (branch `claude/motore-metodo-m1`)

**Funzionato.** Asset Cowork (tabella RPE, seed neurotipo) committati così com'è con test falsificabili sopra (spot-check esatti dalla CSV); la regola "se un test contraddice la tabella vince la CSV" dichiarata da Nick PRIMA di scrivere i test toglie ogni ambiguità; `npx deno test` come runner (Deno non installato sulla macchina): binario ufficiale via cache npm, zero modifiche di sistema, 20/20 verdi.
**Subottimale (+fix).** Il primo run Deno ha creato `deno.lock` alla radice (untracked → rischio stash GHD, lezione nota): rimosso a fine lavoro, run successivi con `--no-lock`. **Fix:** se il motore Deno cresce (M2+), decidere con Nick se adottare `deno.json` + lock versionati.
**Migliorie azionabili.** (1) Prettier riformatta gli asset al commit (wrapping): i VALORI restano intatti e i test lo provano — se mai servisse il byte-identico, `method/` in `.prettierignore` (decisione Nick). (2) Il matching neurotipo→attributi è volutamente minimale (token + sinonimo `esplosivo→balistico`): da arricchire in M2 quando l'orchestrazione dirà quali voci dei seed servono davvero.

### 2026-07-05 — motore-metodo M2: forma B nella edge fn, via l'LLM (branch `claude/motore-metodo-m2`)

**Funzionato.** La review avversariale multi-agente PRIMA della chiusura ha trovato 3 difetti gravi che i test unit non vedevano perché stavano **nei confini** (edge↔FE, motore↔dati di onboarding): il gate che avrebbe bloccato il 100% degli atleti onboardati (`red_flags` è sempre un oggetto a 4 chiavi, anche da sani), la risposta gate/settimana-vuota che il FE trattava come successo **svuotando la settimana del builder**, e `experience_level` scritto in inglese dal FE. La risposta giusta l'ha data Nick (200 `{error, gate:true}` senza `days`): né la forma della spec né la mia proposta 422 — fermarsi sui conflitti di contratto paga.
**Subottimale (+fix).** (1) I moduli erano testati, i **contratti fra i moduli e il mondo** no: i fixture "puliti" non riproducevano la forma reale dei dati (`red_flags` di un atleta sano). **Fix di processo:** per ogni campo letto da `profiles`/jsonb, un test con il valore REALE che il FE scrive (aprire il writer, non immaginare il dato). (2) Schema drift: le colonne method di `exercises` vivono solo sul DB (niente migration-mirror, `types.ts` stale) — flaggato task di riconciliazione (legge #7 + §4-bis).
**Migliorie azionabili.** (1) Task flaggato: migration mirror `exercises` + regen `types.ts`. (2) Restano note (bassa): parsing equipment senza negazioni ("no bilanciere" = bilanciere disponibile), copy del dialog su mode 'continue' ora impreciso, paginazione libreria >1000 righe — da riprendere con l'iterazione FE/M3.

### 2026-07-05 — mirror migration colonne metodo + regen types.ts (branch `claude/confident-lamarr-593702`)

**Funzionato.** Disciplina §4-bis rispettata end-to-end: version esatta (20260704210219) e SQL as-applied chiesti a Cowork via Nick, file mirror committato verbatim senza toccare il DB; il regen di `types.ts` è passato dal **connettore Supabase OAuth** (autorizzato da Nick) quando CLI e MCP locale erano senza token; il primo tentativo CLI scriveva su file temporaneo nello scratchpad → il fallimento non ha corrotto `types.ts`. (Chiude il task flaggato dalla RETRO M2 qui sopra.)
**Subottimale (+fix).** (1) Scoperto in esplorazione che il commit Lovable `2f4c687` ("Changes", 2026-07-05) aveva **regredito** `types.ts` contro il vecchio backend (droppato `appointments`, tolte le circonferenze da `body_measurements`) — fragilità #3 confermata su `main`; il regen l'ha corretta, ma il rischio si ripresenta a ogni sync Lovable finché non c'è il cutover D6. **Fix proposto:** decidere con Nick se disattivare il sync types di Lovable fino al cutover. (2) Tre canali per lo stesso secret (CLI, MCP locale, connettore OAuth) = due tentativi a vuoto prima di quello buono. **Fix:** per il regen types da Code, il connettore OAuth è l'unica via già autenticata senza setup — usare quella per prima.
**Migliorie azionabili.** (1) Follow-up flaggato per M2: sostituire il cast `as unknown as CandidateRow[]` in `generate-program/index.ts:235` con mappatura tipata, ora che `types.ts` conosce le colonne (da fare sul branch M2 o post-merge). (2) `npm run gen:types` disponibile in `package.json` (output da redirigere a mano su `src/integrations/supabase/types.ts`).

### 2026-07-12 — fetta riordino: 7 interventi di verità/pulizia dal report di ricognizione (branch `claude/riordino-repo`)

**Funzionato.** La coppia «ricognizione read-only → fetta di riordino report-driven» regge: ogni intervento aveva già evidenza file:riga nel report (`docs/stato-repo-2026-07-12.md`), zero esplorazione a vuoto in esecuzione; il piano-poi-OK con i 2 punti aperti dichiarati (carve-out hook per `.env.example`; engines node ≥24 dall'ambiente reale) ha evitato rework; il carve-out nell'hook è stato committato PRIMA del file che lo richiedeva, così il guard non è mai stato aggirato; `git branch -d` (mai `-D`) ha fatto da rete: 18 branch eliminati, `claude/oauth-migration` rifiutato (ahead 4 sul suo upstream pur essendo tutto in main) → lasciato e segnalato a Nick.
**Subottimale (+fix).** (1) Il report contava «20 branch mergiati» ma i cancellabili erano 19: il ventesimo era `claude/ricognizione-repo`, in lista NON-toccare — nei prossimi report separare «mergiati» da «mergiati E cancellabili». (2) `git rm --cached .env` preserva la working copy solo nel worktree dove gira: al merge nel checkout principale git ELIMINERÀ il `.env` dal disco (avviso messo nel commit `chore(env)` e da ripetere a Nick pre-merge; l'app resta su `.env.local`). (3) La rimozione del cast `as any` su `program_blocks` ha richiesto comunque un cast puntuale `as unknown as Json` sul campo JSONB (ProgramBlock senza index signature): prevedibile dal report, andava già nel piano.
**Migliorie azionabili.** (1) Al cutover D6, valutare se estendere `.env.example` e citarlo nel README (fetta futura). (2) `claude/oauth-migration`: Nick decide se `-D` locale o push dell'allineamento e poi cancellare anche il remoto. (3) Restano fuori scope e flaggati: riferimenti «PWA» residui in CLAUDE.md §2/§8 e nel titolo di `methodology/02` (coerenza da sistemare quando si tocca la corsia athlete).

### 2026-07-12 — fetta F0 fondamenta: schema+RLS da spec CORE + doc allineata (branch `claude/f0-fondamenta`)

**Funzionato.** Piano-poi-OK con i 2 punti aperti dichiarati (ON DELETE delle FK nuove; apply sì/no) → decisioni nette di Nick PRIMA di ogni DDL, zero rework; ricognizione parallela pre-piano (convenzioni migrazioni/policy, collisioni nomi, firma `is_coach_of_athlete`, realtà di `daily_readiness`/`useFeatureAccess`) = zero esplorazione a vuoto in esecuzione; review indipendente a fine fetta (rls-auditor + 3 verificatori avversariali: conformità-spec, correttezza SQL, verità-doc) = 4/4 PASS, 0 blocker, ma ha trovato 3 imprecisioni-doc reali chiuse prima del merge (gate booleani di `useFeatureAccess` omessi; residuo PWA in CLAUDE.md §7; sintesi «SELECT own/coach» di `audit_log` più ampia della policy reale). Pattern RLS migliorativo adottato senza attrito: SELECT combinata own-or-coach + `(SELECT auth.uid())` → non aggrava i finding advisor `multiple_permissive_policies`/`auth_rls_initplan`.
**Subottimale (+fix).** (1) Lo sweep-PWA della spec elencava §2/§8 ma un residuo vero stava in §7 (tabella file-metodologia): gli sweep di termini si fanno col grep sull'INTERO file, non sulla lista puntuale della spec. (2) La policy SELECT di `audit_log` è actor-centric: il coach NON vede gli accessi di terzi ai dati del suo atleta (`entity_id` fuori dalla policy) e le righe anonimizzate (`actor_id` NULL post SET NULL) sono invisibili a tutti i client — coerente col design F0, ma la fetta che consumerà il log (reporting accessi-dati-salute, CORE §6) dovrà rivalutarla.
**Migliorie azionabili.** (1) Per Cowork post-merge (opzione B di Nick): apply delle 6 migrazioni `20260712150000..150005_f0_*` + seed `method_config` + fixture + `get_advisors(security)` + verifica-live; POI regen `types.ts` in una sessione Code successiva (in questa fetta volutamente NON rigenerato). (2) Debiti nominati: retention/minimizzazione del `payload` jsonb di `stripe_events` → fetta F3 · divergenza lessicale CORE §4 {premium, _mensile_} vs enum reale {premium, _monthly_} → riconciliare nel CORE (Cowork) · CASCADE su `consents` = decisione Nick 2026-07-12 (valore probatorio del consenso post-cancellazione → export pre-delete da valutare nella fetta GDPR-erasure).

### 2026-07-13 — zoneMap v18: split dorsale/toracica + clamp ZONE_BASE + conform FASE B (branch `claude/zonemap-v18`)

**Funzionato.** Spec con delta «completi ed esatti» = zero reinterpretazione in esecuzione; la ricognizione pre-piano ha scoperto che il branch vivo `claude/zonemap-riconc` era una **bozza divergente** dalla spec riconciliata (`mid back`→toracica, alias extra `lat`/`gran dorsale`, senza Delta D) → branch nuovo da `main` invece di cherry-pick, evitata una regressione silenziosa al merge; la review avversariale multi-agente (3 lenti + refuter con **mutation testing**) ha trovato un buco reale che i 49 test verdi non vedevano: il Delta A (payload principale della fetta) era integralmente revertibile a suite verde — il pin «toracica tiene hip hinge» passava in modo **vacuo** via `if (!rule) continue;` — chiuso con 3 assert discriminanti e riverificato sul mutante (ora fallisce in 2 punti).
**Subottimale (+fix).** (1) I 4 test pinnati della spec erano necessari ma non sufficienti: un'asserzione **negativa** («X è tenuto») non pinna l'esistenza della regola che dovrebbe tenerlo. **Fix di processo:** per ogni delta di contenuto almeno un assert che fallirebbe sul revert (mutation-check, anche solo mentale), e trattare la lista-pin di una spec come minimo, non tetto. (2) `deno.lock` ricreato alla radice del worktree da un run senza `--no-lock` (probabile sub-agente di review): rimosso — lezione M1 confermata; istruire anche i sub-agenti sul flag.
**Migliorie azionabili.** (1) `claude/zonemap-riconc` da eliminare **senza merge** (locale + origin) — segnalato in `HANDOFF §0`, decisione Nick. (2) Deploy `generate-program` 17→18 **ESEGUITO** al "procedi" esplicito di Nick (version 18 ACTIVE, `verify_jwt=true`, connettore OAuth — l'MCP locale era senza token: il connettore resta la prima via, lezione 2026-07-05 confermata); resta il merge del branch, push da GitHub Desktop (il classifier blocca il push dalla corsia Code anche con OK in chat: da sapere per le prossime fette).

### 2026-07-14 — fetta intake-form-fase2: form config-driven + cutover + ritiro wizard (branch `claude/intake-form-fase2`)

**Funzionato.** (1) Il **test di parità col validatore server REALE** (importare `validate.ts` — puro, senza Deno API — dentro vitest e asserire `ok:true` sul payload costruito dal client) è il pin di contratto più forte mai usato qui: qualunque drift client↔server fallisce in unit, senza mock. (2) Import diretto dei moduli single-source della edge fn nel bundle FE (zone/ENUMS/DCA/PAR-Q/OBJECTIVES, tutti import-free): zero enum ricopiati, `allowImportingTsExtensions` già pronto in tsconfig. (3) La review avversariale di fine fetta (4 lenti + verificatori scettici) ha trovato 4 major VERI che 28 test verdi non vedevano — tutti nei CONFINI (draft↔auth, copy↔pulizia draft, test↔mutazioni): confermata la lezione M2/v18 che i difetti vivono fra i moduli, non dentro. (4) Pre-flight `list_migrations` via connettore PRIMA del regen types: la condizione della memoria («regen solo post-apply») verificata coi fatti, non assunta.

**Subottimale (+fix).** (1) Config scritta senza stimare le righe POST-prettier: 764r al primo colpo (legge 10), split in 4 moduli DOPO — la lezione Fase 1 («stimare le righe PRIMA») va applicata anche ai file-dato, con l'aggravante che prettier espande ~3×. (2) Il primo design dello store draft ignorava il ciclo di vita dell'ACCOUNT (chi possiede la bozza? cosa succede al logout/cambio utente?): su dati art. 9 il modello di ownership del client-storage va deciso al design, non alla review. (3) Ho pinnato l'ordine dei 30 item con 2 checksum «per brevità»: su un contratto POSIZIONALE il pin o è integrale o è vacuo (stessa classe della lezione mutation v18).

**Migliorie azionabili.** (1) Standard per i prossimi form/questionari: ogni chiave localStorage con dati utente dichiara OWNER (user id) + punto di pulizia in `signOut` + deep-merge sul persist. (2) Pattern «parità col validatore server» riusabile per ogni futura edge fn con validatore puro: promuoverlo a prassi nei prompt-file (sezione Verifica). (3) Restano per fette future: E2E submit reale con atleta di test (Nick) · approvazione testi-consensi hub-v1 (Nick, pre-merge) · hardening Cowork su clearance/red_flags (post-merge, ora sbloccato) · rimozione import morto `SunThemeSync` (task flaggato).

### 2026-07-15 — fetta allenamento-autonomo: rilascio autonomo del primo programma (branch `claude/allenamento-autonomo`)

**Funzionato.** (1) Piano-poi-OK con le decisioni dichiarate (mappa objective→driver, semaforo giallo=STOP, blocco conservativo −1 set) → 2 correzioni nette di Nick PRIMA del codice (consenso art.22 = lista a 3 con `ai_processing` già nell'enum; consolidamento dei gate sovrapposti) e zero rework. (2) Il **characterization test generato PRIMA del move** (golden master byte-identico su 3 scenari) ha reso l'estrazione in `_shared/method` falsificabile invece che "fidati del git mv". (3) La review avversariale multi-agente (5 lenti + 2 refuter per finding) ha trovato di nuovo i difetti **nei confini** che 139 test verdi non vedevano: un consumer del path vecchio fuori dal perimetro della fetta (`zones.test.ts` di submit-intake), il mirror FE più severo del gate autorevole (clearance che non riapre in UI), il flood di alert senza dedupe, e 2 mutazioni sopravvissute nei test (floor conservativo non pinnato; assert auto-referenziale su `stopFor`). (4) Conflitto istruzione-utente ↔ codice (chiave `yellow` vs `yellowSignals`): risolto aprendo il WRITER reale (lezione M2) e dichiarandolo con file:riga — lettura difensiva su entrambe le chiavi.
**Subottimale (+fix).** (1) Il "move meccanico" ha migrato solo il consumer che conoscevo (`generate-program/index.ts`): il grep dei consumer del path vecchio andava fatto PRIMA del commit di move, non scoperto dalla review. **Fix di processo: ogni move/rename → `grep -r "<path-vecchio>"` sull'intero repo come acceptance del commit stesso.** (2) La cwd della shell è tornata al repo principale fra i turni e un `sed` con path relativi ha modificato 2 file del MAIN (ripristinati subito con checkout): **nei worktree, comandi di scrittura solo con path assoluti o `cd` ripetuto nello stesso comando.** (3) Un'asserzione che confronta l'output con una funzione che legge lo STESSO catalogo (`stopFor`) è un pin vacuo — stessa classe della lezione v18: il pin o è letterale o non è.
**Migliorie azionabili.** (1) Per Cowork/Nick al merge: secret `SAFETY_NET_COACH_ID` + deploy `release-autonomous-program` + REdeploy `generate-program` (importa `_shared/`) + smoke coach + seed `ai_processing` per l'atleta di test (dettaglio in `HANDOFF §0`). (2) BLOCCO GO-LIVE nominato: un-park + raccolta `ai_processing` all'intake. (3) Debiti nominati: release+audit non transazionali · equipment non riconosciuto = palestra piena (whitelist 28 nella UI intake) · rate-limit tentativi · raccordo FK workout_logs→release (ID stabili già pronti).

### 2026-07-13 — fetta intake-redesign Fase 1: backbone dati+sicurezza+invito+submit (branch `claude/intake-redesign`)

**Funzionato.** Ricognizione multi-agente pre-piano (8 lenti parallele: contratto nc-questionnaire, scoring neurotipo, regola-obiettivo-arena, wizard Hub, flusso invito, migrazioni, gate del motore, stato FE/types) + verifica LIVE dello schema (trigger `handle_new_user`, RLS, enum, colonne) = piano senza esplorazione a vuoto e 3 correzioni di realtà al prompt-file (zone canoniche = **15**, non 14 · semaforo/DCA/dolore-gesto ASSENTI dal contratto sorgente → definiti con Nick · `calibration_requirements` non consumata dal motore). Piano-poi-OK con 5 punti aperti (A–E) → decisioni nette di Nick + 1 correzione sostanziale (split `condition`/`medications`) PRIMA di ogni riga di codice. Scoring neurotipo portato 1:1 dal TS già validato di nc-questionnaire con i 3 `validation_examples` come regressione (il requisito §J era soddisfatto by-design: cercare il port esistente prima di riscrivere paga). **Review `supabase-rls-auditor` a fine fetta: PASS sulle 3 verifiche fisse (deny-by-default · append-only consents/audit · submit autenticato self-only), 0 blocker**; il warning 1 (metadata onorati anche nel ramo `invite_tokens`) era REALE e chiuso in-branch prima del merge (`eb93fe7`). 54 test Deno verdi (`--no-lock`, lezione M1 rispettata: nessun `deno.lock` residuo).
**Subottimale (+fix).** (1) `validate.ts` nato a 475 righe (>300, legge 10) e splittato DOPO in 3 file coesi (`validateSpec`/`validateParts`/`validate`): la whitelist di un contratto B0–B8 non sta in un file solo — stimare le righe PRIMA di scrivere un validatore. (2) L'accettazione-di-rischio scritta nel commento del trigger non copriva tutti i rami che onoravano i metadata (warning 1 dell'auditor): quando si dichiara un rischio residuo, enumerare i RAMI coperti, non "la funzione" — l'auditor indipendente l'ha visto, io no.
**Migliorie azionabili.** (1) Per Cowork post-merge: apply delle 3 migration `20260713150000..150002_intake_*` + `get_advisors(security)` + **deploy `submit-intake`** + **REdeploy `invite-athlete`**; POI regen `types.ts` in sessione Code (un solo regen copre anche F0). (2) Debiti nominati: **hardening trigger anti-escalation** (`coaching_mode`/`tier`/`medical_clearance_required`/`red_flags`) = **GATE DI GO-LIVE Fase 2** (HANDOFF §0) · metadata-trust di `handle_new_user` (un self-signup che conosce l'UUID del coach può claimare `tier` — decisione Nick, warning auditor) · rate-limit sul ramo-minore di `submit-intake` (flood `audit_log`/`coach_alerts`) · log scrubbing di `invite-athlete` (nota auditor, preesistente). (3) Fase 2: rendering config-driven del form, 30-Q nella UI, selettore zone/infortuni, rewire `OnboardingWizard` su `submit-intake`, ritiro `nc-questionnaire` + flusso `invite_tokens`.

### 2026-07-15 — fetta audit-consent-blocked: traccia audit art.22 sul ramo consenso (branch `claude/audit-consent-blocked`)

**Funzionato.** (1) Prompt-file di Cowork con contesto già verificato live (E2E sess.69) + Plan-first: la ricognizione parallela pre-piano (4 lenti: schema `audit_log`, consumer FE, harness test, delta-doc) ha istruito le 2 decisioni del piano coi FATTI — best-effort confermato dal codice FE (branch sul body: un 500 sostituirebbe il prompt di consenso con l'errore generico) e builder testato come unico pin non-vacuo (nessun test del repo importa mai un `index.ts`: `serve()` top-level). (2) OK di Nick con vincoli netti PRIMA del codice (asimmetria dichiarata nel commit, colonne reali, acceptance E2E su branch) = zero rework. (3) Review avversariale di fine fetta (4 lenti + 2 refuter per finding): 0 confermati su 7 sollevati — su una fetta minima con ricognizione a monte i confini erano già coperti.

**Subottimale (+fix).** (1) Il build-gate del hook pre-commit gira nella cwd del repo PRINCIPALE, non nel worktree: `node_modules` stale su main (vitest mancante dopo i merge delle fette recenti) ha bloccato il commit del worktree con un rosso NON correlato alla fetta. **Fix: dopo ogni merge che tocca `package.json`, rifare `npm install` anche nel checkout principale** — il gate del hook lo usa. (2) I worktree nuovi non ereditano `node_modules`: `npm test`/`tsc` locali richiedono `npm install` in-loco (pattern già di fatto usato da allenamento-autonomo ma mai scritto — ora lo è).

**Migliorie azionabili.** (1) Per Cowork/Nick al merge: REdeploy `release-autonomous-program` + acceptance E2E su **branch Supabase** (1 riga `audit_log` `autonomous_release_consent_blocked` con `metadata.missing` corretto, 0 `coach_alerts`, risposta invariata; query nel messaggio di chiusura sessione). (2) Debito flood `audit_log` esteso al ramo consenso, registrato in HANDOFF §0. (3) Flaggato fuori scope (chip): trigger anti-mutazione assente su `audit_log`/`consents` — l'append-only non è enforced a DB, a differenza di `program_releases`.

### 2026-07-17 — fetta nutrition-v1-closed-loop: motore adattivo + rilascio immutabile + persistenza pasti (branch `claude/nutrition-v1-closed-loop`)

**Funzionato.** (1) Le Appendici A/B mancavano dal prompt → recuperate dalla **fonte di verità live** (SQL as-applied da `schema_migrations.statements` + riga `method_config` via connettore read-only, pre-flight come da lezione 2026-07-14): il file-specchio è **verbatim per costruzione**, non ricostruito a mano. (2) AskUserQuestion coi 4 punti aperti lanciata MENTRE i 2 agenti di design lavoravano in background → risposte nette di Nick (inclusa una correzione sostanziale di sicurezza: `exercise_kcal_per_day_estimate` **300, non 0** — con 0 il gate LEA si indebolisce) senza tempo morto, zero rework a valle. (3) I **pin numerici calcolati a mano PRIMA di scrivere il codice** (EMA, evidenza, gain, cap, pavimento): 69 test verdi al primo run — il costo del calcolo anticipato si ripaga in debugging zero; il pin anti-mutazione dedicato alla correzione di Nick (config 300→0 flippa l'esito del test LEA) rende la sua decisione falsificabile. (4) La decisione "config v2 prima" (parse STRICT fail-loud) ha eliminato per costruzione la classe "costante hard-coded dimenticata": il grep di acceptance non ha trovato nulla perché non c'era nulla da trovare.

**Subottimale (+fix).** (1) Due file oltre le 300 righe scoperti solo col `wc -l` POST-scrittura (`assemblePlan.ts` 315, `processAthlete.ts` 331) → split a posteriori (`coldStart.ts`, `fetchInputs.ts`): la lezione intake-form-fase2 («stimare le righe PRIMA di scrivere») confermata per la terza volta — per i moduli-orchestratore pianificare lo split in fase di design, non di refactor. (2) L'estensione della decisione Q3 di Nick (3 parametri → 6, aggiungendo i 3 numeri strutturali emersi dal design del motore) è stata dichiarata nel piano e approvata, ma è un pattern da maneggiare con cura: un'estensione di scope decisa dall'agente va SEMPRE resa esplicita con un'etichetta («estensione dichiarata»), mai infilata in silenzio in una lista.

**Migliorie azionabili.** (1) Go-live nutrition per Cowork/Nick in `HANDOFF §0`: seed `nicolo_nutrition` **v2** (6 campi, valori decisi da Nick) → attivazione `is_active` → deploy 2 fn → smoke → SOLO POI scheduling settimanale. (2) Review avversariale multi-agente = passo successivo VINCOLATO (ordine di Nick: la fetta si dichiara chiusa solo a valle dell'esito, fix in-branch). (3) Debiti nominati in HANDOFF: transazionalità release+plan+audit · fonte dati reale per kcal-esercizio · UI rilasci nutrizionali · pesi da `daily_readiness.body_weight` ignorati (spec-fedele, ma è un segnale in più disponibile).

### 2026-07-16 — fetta unpark-ai-consent: 7° consenso opt-in + prompt in-app + RPC record_consent (branch `claude/unpark-ai-consent`)

**Funzionato.** (1) Ricognizione parallela pre-piano (5 lenti) → trovata la fixture Deno hardcoded (`coachedBody()` a 6 consensi, NON elencata nella spec: avrebbe fatto cadere in blocco i 15 test di `validate.test.ts`) e verificato che il gate della release è **version-agnostic** → il bump hub-v2 deciso senza rischio. (2) Piano-poi-OK con 5 punti aperti → risposte nette di Nick + un'aggiunta di scope da lui motivata (DROP `consents_insert_own`) con pre-condizione falsificabile («grep insert client = 0, altrimenti STOP») verificata PRIMA di scrivere la migration. (3) La review avversariale (4 lenti + 2 refuter per finding, 12/20 confermati al netto dei duplicati) ha trovato di nuovo i difetti **nei confini**: la race cancel↔RPC-in-volo che produceva un toast GDPR contraddittorio («non concesso» su un consenso in corso di registrazione, con rilascio a seguire), e il **pin vacuo** di `CONSENT_VERSION` — la mutazione hub-v2→hub-v1 sopravviveva a tsc + 110 test: la lezione v18 vale anche per le COSTANTI-contratto, non solo per gli array posizionali.

**Subottimale (+fix).** (1) Il pin letterale di `CONSENT_VERSION` andava scritto nello STESSO commit del bump: regola nuova — ogni costante-contratto che una fetta bumpa riceve il suo assert letterale contestualmente. (2) Montare un dialog solo in ALCUNI rami di una view a guard-return crea stato orfano by-design (refetch in background → ramo cambia → dialog sparisce con `open=true` latente): pattern `withPrompt` (mount in ogni return) da riusare. (3) Un Edit su una regione appena committata è atterrato su testo riformattato da prettier (hook PostToolUse) ed è fallito a metà: re-Read prima di editare regioni appena passate dal commit-hook.

**Migliorie azionabili.** (1) Per Cowork/Nick al merge (⚠ dopo il gate legale G9 sul testo bozza): ordine **deploy-insieme** vincolante in HANDOFF §0 (apply `20260716170000` → deploy `submit-intake` → publish FE; `release-autonomous-program` NON si rideploya) + acceptance E2E della RPC e del gate via connettore su branch Supabase; POI regen `types.ts` in sessione Code (rimuove i cast `as never`). (2) Per il track G9: il testo bozza promette la revoca ma il percorso non esiste (art. 7(3)) — riconciliare testo o feature alla validazione. (3) Chip flaggate: commento stale `release/consents.ts` (file vietato in questa fetta); debito replay/no-dedupe di `record_consent` registrato in HANDOFF (famiglia rate-limit).
