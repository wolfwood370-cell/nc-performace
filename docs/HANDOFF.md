# HANDOFF — nc-performance-hub (trasferimento sessione)

> **Aggiornato:** 2026-07-19 (Claude Code, fetta reinvio-inviti) — **stato vivo nell'Addendum §0 qui sotto**, che prevale su §2/§4 dove diverge; fotografia completa in `docs/stato-repo-2026-07-12.md`. Stato 2026-06-18: Migrazione Lovable→Supabase di proprietà avanzata: smoke AI 6/6, flusso invito→atleta verificato, fix cache roster, **`generate-program` cablata nella UI (D11)**, **D12 Set A pulizia dead-code ACWR fatto**, **dual-agent Code/Cowork formalizzato (`COWORK.md`, `69e8a4a`)**, **campagna autonoma D13 COMPLETA** (7 hook WIP collegati + E2E verde + audit pulito). Codice su `origin/main` (tip `2296074`).
> **Priorità aperta (aggiornata 2026-07-13):** la ex "libreria esercizi vuota" è **SUPERATA** — `exercises` = **954 righe** sull'Hub (v. Addendum §0). Vive ora: cutover D6 `.env` + merge dei 2 branch vivi (`claude/zonemap-v18` — **v18 già LIVE** sull'Hub — e `claude/invite-resend-hardening`) + eliminazione senza merge di `claude/zonemap-riconc` (superseded).
> Scopo: riprendere da una chat nuova (Cowork **o** Claude Code) senza perdere contesto. Prompt pronti in §8 (Claude Code) e §9 (Cowork).
> ✅ **Stash-loop GitHub Desktop CHIUSO (2026-06-17):** `HANDOFF.md` + piani D11/D12 (`f6bd4b2`) e i doc di riferimento D2/D3/D7/D8/D9/D10/PRODUCT_SPEC/ROADMAP/… (recuperati dagli stash GHD, `1de3b7f`) sono ora **trackati**. Restano 3 stash GHD da droppare dopo che Nick mette al sicuro `.env` — vedi §5.

---

## 0. Addendum 2026-07-19 (stato vivo — prevale su §2/§4 e sugli addendum precedenti dove diverge)

- **Fetta reinvio-inviti (branch `claude/invite-resend-v2`, 2026-07-19, da mergiare — chiude il parcheggio di `claude/invite-resend-hardening`)** — re-port chirurgico per IDEE del branch stantio 82fc631 sopra il main attuale (mai mergiato/rebasato). Ramo already-exists di `invite-athlete` riscritto: lookup **READ-ONLY** `listUsers` **paginato completo** (nextPage-driven, cap di sicurezza — via il limite perPage=200 e i fall-through silenziosi di main; il magiclink si genera SOLO nei rami che inviano, DOPO decisione e scritture — variante (c) di Nick, v. sotto), decisione nel helper PURO `invite-athlete/invite/decision.ts` (6 esiti; guardia account-attivi su OGNI ramo: solo `onboarding_completed===true` blocca il reinvio — il branch stantio la applicava al solo ramo alreadyLinked), reinvio col template condiviso `inviteEmail()`, errori select/insert/update propagati 500 espliciti, 502 senza details, link MAI in response/log (scartato sugli esiti no-resend), `resent` additivo in risposta (ramo primario invariato `{success,email,fullName}`; mode/tier only-if-supplied preservati). Dialog coach: copy TRI-STATE su `resent` (true=reinviato · false esplicito=account attivo garantito · assente=fn vecchia → copy conservativa senza claim di attivazione). Verifica: **287 Deno verdi** (279 baseline misurata + 8 helper) · vitest 89/89 · tsc · `deno check` su index.ts (gate nuovo: chiude la classe wiring-non-testabile della lezione api-key) · acceptance: `listUsers` SOLO nel lookup paginato con doc-comment read-only (divieto della spec REVOCATO da Nick: era motivato dal perPage=200) · nessun `generateLink` magiclink prima della decisione (r.316 > r.298) · raggiungibilità no-send verificata da auditor avversariale (tabella esito→sequenza: attach-no-resend/no-resend-active/conflict = zero generateLink) · `details`=0 · chiavi legacy=0 · diff = 4 file codice + 3 docs. **Review avversariale eseguita** (36 agenti: 5 lenti + 8 mutazioni su copia scratchpad — 8/8 uccise — + 2 refuter/finding, soglia sui voti effettivi): 3 finding chiusi in-branch (`1c2f19c` pin vacuo + tabella completa, `81400e2` copy mixed-version); 7 refutati spec-based. **1 MAJOR CONFERMATO e RISOLTO in-branch (`b41f3f4`, variante (c) decisa da Nick 2026-07-19)**: il lookup magiclink-as-lookup della spec RUOTAVA il `recovery_token` GoTrue del bersaglio anche sui rami senza email (409 incluso — magiclink e recovery condividono lo slot in GoTrue; `forgot-password` ne dipende) → un coach poteva invalidare ripetutamente un reset-password pendente di un utente arbitrario. Fix: lookup side-effect-free (`listUsers` paginato completo) + `generateLink(magiclink)` spostato dentro `resendInvite`, invocata SOLO dai 3 esiti che inviano, dopo decisione e scritture profilo; invio fallito → 502 col retry del coach idempotente su resend-pending. Zero side-effect auth sui rami no-send. **POST-MERGE (Nick)**: `npx supabase functions deploy invite-athlete --project-ref xgxtplqlewpqjzghvbke` + prova reinvio con l'atleta di prova (chiude anche il caveat magiclink-su-email-non-confermata, addendum §3 del prompt-file `docs/prompts/2026-07-19-reinvio-inviti.md`) + chiusura branch vecchio: `git branch -D claude/invite-resend-hardening` (solo locale, mai mergiato — `-D` obbligatorio).
- **Fetta email-brand (branch `claude/email-brand`, 2026-07-19, da mergiare)** — email transazionali unificate sul brand NC: nuovo modulo PURO `supabase/functions/_shared/email/templates.ts` (layout unico: header navy `#003e62` + filetto `#226fa3`, card 560px, footer differenziato per email; costruttori `inviteEmail`/`recoveryEmail` → `{subject, html}`; `escapeHtml` spostato qui e applicato a OGNI valore interpolato nel HTML, actionLink incluso; subject NON escapato per design — la guardia CR/LF vive nel chiamante, contratto dichiarato nel doc-comment). `invite-athlete` e `forgot-password` usano il modulo (HTML inline sparito); `forgot-password` ora spedisce da `NC Training Systems <noreply@mail.nctrainingsystems.com>` (prima `onboarding@resend.dev`); i 502 perdono `details` (dettagli Resend solo nel log server — unica variazione di contratto, dichiarata). **`send-email` RITIRATA** (dir + blocco `config.toml`; brand vecchio "Coach Athlete Hub", 0 chiamanti FE). Contratti request/response INVARIATI; whitelist redirectTo e anti-enumeration INTATTE; grep chiavi legacy in functions/ = 0 (invariato). Test: **279 Deno verdi** (265 baseline misurata + 13 modulo + 1 characterization golden byte-identica; fedeltà al preview Cowork `app/preview-email-brand-2026-07-19.html` verificata con confronto automatico) + tsc verde. **Review avversariale eseguita** (41 agenti: 5 lenti + 10 mutazioni su copia scratchpad + 2 refuter/finding): 10/10 mutazioni uccise, **0 major nel codice spedito**; 3 finding di qualità-test chiusi in-branch (`0742c0b`); nota PRE-ESISTENTE flaggata come chip: i path 500 (generateLink/catch-all) di entrambe le fn restituiscono ancora il message interno al client — fetta dedicata. **POST-MERGE (Nick)**: `npx supabase functions deploy invite-athlete forgot-password --project-ref xgxtplqlewpqjzghvbke` + **`npx supabase functions delete send-email --project-ref xgxtplqlewpqjzghvbke`** (il deploy NON rimuove la fn dal progetto remoto) + smoke: invito reale ed email di recovery arrivano col brand NC dal sender proprio. **Branch `claude/invite-resend-hardening` PARCHEGGIATO** (decisione Nick 2026-07-19): solo locale, 4 commit, 133 dietro main (pre-F0, pre-api-key) → re-port su main in fetta dedicata; il modulo template gli semplifica il re-port (l'helper del branch chiamerà `inviteEmail`). Doc con riferimenti stale a `send-email` (`DB_MIGRATION`, `PRODUCT_SPEC`, `SECRETS_SETUP`, `methodology/03`, `.lovable/plan.md`) = chip corsia doc.
- **Fetta auth-flow-fix (branch `claude/auth-flow-fix`, 2026-07-19, da mergiare)** — 3 fix del flusso auth su Vercel: (1) `vercel.json` alla root con rewrite SPA→`index.html` (full-page load di rotte interne — callback OAuth `/auth`, link email `/reset-password` — non è più 404); (2) whitelist `redirectTo` di `forgot-password` sul dominio `nc-performace-mu.vercel.app` (via `LOVABLE_PROJECT_ID` e ramo `lovable.app`; localhost http dev e obbligo HTTPS invariati) — **✅ DEPLOYATA: `forgot-password` v22 live dal 2026-07-19** (NB: la fetta email-brand qui sopra richiede un NUOVO redeploy della stessa fn al merge); (3) hardening ruolo signup: migration `20260719120122_harden_signup_role_athlete_only.sql` **✅ APPLICATA live il 2026-07-19** (Cowork col benestare di Nick — DDL verbatim dal live TRANNE il ramo finale: il role client-supplied non viene mai letto, sempre `athlete` + `onboarding_completed=false`; rami invito `meta_coach_id`/`invite_tokens` intatti) + FE senza role picker (`useAuth.signUp` a 3 argomenti, signup pubblica sempre atleta → `/onboarding`, invito → `/athlete`; la creazione coach resta amministrativa). NO regen types (nessun cambio schema). Verifica: `tsc` verde · vitest 89/89 · **Deno 265/265 invariata** · e2e `auth-page.spec.ts` aggiornata (asserisce l'ASSENZA del picker). Fuori scope dichiarato (decisione Nick): whitelist Lovable + `DEFAULT_ORIGIN` in `create-checkout-session`/`create-portal-session`/`send-email` → fetta billing con E2E propri.
- **Fetta api-key-migration (branch `claude/api-key-migration`, 2026-07-19, merge-ready)** — Fase B della migrazione API key: **18/18 edge fn sulle chiavi nuove** via NUOVO `supabase/functions/_shared/apiKeys.ts` (`publishableKey()`/`secretKey()` fail-fast, ZERO fallback legacy; selezione `default` con fallback su chiave unica loggando il NOME mai il valore — nota di Nick; 14 test con pin anti-leak). **Re-pattern `compute-nutrition-target`**: gate `x-cron-secret` vs env `CRON_SECRET` (timing-safe, modulo puro `target/cronAuth.ts`, 9 test: 401/403/500 + pin letterale del nome header), `verify_jwt=false` (la sb_secret è opaca, il gateway JWT la rifiuterebbe); contratto body/risposte INVARIATO. `config.toml` **pinna 18/18** (aggiunta `submit-intake`; correzione di realtà: `forgot-password` era GIÀ pinnata `false` nel repo — la spec §2 diceva 16/18, il reale era 17/18). Acceptance verificata: grep legacy = **0** (commenti inclusi) · **265 test Deno verdi** (baseline misurata 242 + 14 apiKeys + 9 cronAuth) · `tsc` verde · diff = solo i 23 file dichiarati · nessun `deno.lock`. **Review avversariale eseguita** (28 agenti: 5 lenti + 11 mutazioni su copia scratchpad + 2 refuter/finding): 1 major VERO chiuso in-branch (`f7ad773`: import mancante in ask-copilot — unica su 18, ReferenceError→500 su ogni chiamata al deploy; invisibile a 264 test verdi + grep) + mutazione M5 sopravvissuta chiusa con pin dedicato e riuccisa sulla copia (11/11); 6 finding refutati spec-based. **POST-MERGE (Nick)**: pre-flight §2 della spec (secret key nel Dashboard — nome `default` o unico, il fallback ora tollera; `CRON_SECRET` settato via `supabase secrets set`) → `npx supabase functions deploy --project-ref xgxtplqlewpqjzghvbke` (bulk, rispetta config.toml) → smoke §Verifica (no header→401 · header errato→403 · header giusto→batch ok, sparcheggia lo smoke E2E nutrition di sess.75) → **Fase C**: disable legacy keys dal Dashboard (neutralizza la service_role esposta in sess.75; rollback = re-enable a un click). **Cowork**: verifiche live post-deploy e post-C (spec §Verifica punti 1-5).
- **Fetta nutrition-review-notify (branch `claude/nutrition-review-notify`, 2026-07-18, OK di Nick — merge-ready)** — pausa atleta nel motore `compute-nutrition-target` (accende lo stato "in revisione" della UI nutrition già in main, finora dormiente): su ogni gate di sicurezza NON-consent (`safety_capture`/`unintended_weight_loss`/`low_energy_availability`/`anomalous_adjustment`), DOPO il coach alert riuscito, insert `notifications` `type='nutrition_review'` (`sender_id` null, message neutro identico per ogni motivo — zero dettaglio clinico art. 9, `link_url` `/athlete/nutrition`); su rilascio pulito (cold start e lifecycle-referral inclusi) le non-lette passano `read=true` (igiene campanella, best-effort). **Guardia anti-doppione RELEASE-AWARE** = stesso predicato della pausa UI: skip solo se esiste non-letta con `created_at` > ultimo `released_at` ASSOLUTO (COALESCE floor 1970 al primo blocco; una stantia da clear fallito NON sopprime — riparte a ogni ciclo). Anchor: `consent` → nessun lookup · `safety_capture` → `fetchLatestReleasedAt` dedicato (blocca PRIMA del fetch motore) · rami engine → `previousReleases[0] ?? fetchLatestReleasedAt` (la catena fetchata è windowed ~98gg — 2 errori della spec originale corretti aprendo il codice, riconosciuti da Nick). **Decisione Nick registrata**: errore sull'anchor → la guardia degrada a "qualsiasi non-letta sopprime" (NIENTE fail-open: anti-spam; missed-hold = rarità composta, dichiarato nel doc-comment). Contratto request/response INVARIATO, hold-only (mai via `raiseAlert`), `coach_alerts`/`audit_log` intatti, zero DDL, zero `src/**`. Test: 26 Deno (10 unit + 12 wiring col motore puro reale + 4 alerts). **2 review avversariali eseguite** (fetta: 22 agenti, 10/10 mutazioni uccise, 5 finding chiusi; delta release-aware: 14 agenti, 6/6 mutazioni, 5 finding chiusi — le 9 mutazioni-sopravvissute scoperte dai refuter rifatte morire post-fix). **POST-MERGE (Cowork/Nick): REdeploy `compute-nutrition-target`** — nessuna migration, nessun FE; poi smoke LIVE del giro completo su atleta di test: has_pain → notifica → guardia release-aware (2ª run: nessun doppione) → clear su rilascio pulito.
- **Fetta nutrition-v1-closed-loop (branch `claude/nutrition-v1-closed-loop`, 2026-07-17, da mergiare)** — motore di adattamento nutrizionale closed-loop (Autonoma), NIENTE UI. Schema GIA' applicato live (`20260717125453_nutrition_v1_closed_loop_schema`: WORM `nutrition_releases` gemello di `program_releases`, `nutrition_logs` +`photo_url`/`confidence_score`/`source`, **`meal_logs` DROPPATA**) — nel repo c'è il **file-specchio verbatim** (recuperato da `schema_migrations.statements`, MAI riapplicare). Nuovo modulo puro **`_shared/nutrition/`** (12 moduli, 69 test Deno: EMA gap-aware, evidenza dispendio con imputazione, confidenza a 3 componenti in media geometrica, gain liscio, fallback solo-peso, guardrail escalation>cap>pavimento-macro, streak diet-break, gate LEA/calo-involontario, cold-start maintain forzato; adherence-neutral pinnato). Nuova edge fn **`compute-nutrition-target`** (service-role only: verify_jwt + match esatto del service key; body `{athlete_id?}` singolo/batch CONCURRENCY 3; consenso `nutrition_advice` → cattura `has_pain` sull'ULTIMO `daily_readiness` (decisione Nick, senza finestra recency) → motore → INSERT `nutrition_releases` con `supersedes_id`, 23505→conflict; `nutrition_plans` scritto SOLO al cold-start, `coach_id` NOT NULL con fallback `SAFETY_NET_COACH_ID`; alert `nutrition_safety`/`low_energy_availability`/`female_lifecycle_referral` con dedupe del gemello, referral una-tantum ASSOLUTA anche se dismissed; audit `nutrition_target_blocked` best-effort / `nutrition_target_released` fail-loud, `actor_role='system'`). **`analyze-meal-photo` ora PERSISTE**: insert `nutrition_logs` sotto JWT-atleta (RLS insert-own, no service_role), `source='photo_ai'`, `confidence_score` 1-100, `date` Europe/Rome; request invariato, risposta additiva `persisted:boolean`. `types.ts` rigenerato (via `meal_logs`, dentro `nutrition_releases` + RPC `record_consent` — recupero del drift unpark). Test: 69+4 Deno nuovi + suite esistenti + 57 vitest + tsc verdi.
- **⚠ GO-LIVE nutrition (ordine vincolante, Cowork col benestare di Nick)**: (1) **seed `method_config` `nicolo_nutrition` v2** = v1 + i **7 campi** che il parse STRICT richiede (valori proposti, li decide Nick): `exercise_kcal_per_day_estimate: 300` (**correzione esplicita di Nick: NON 0** — con 0 il gate LEA gonfia la EA e perde atleti a rischio RED-S), `unintended_weight_loss_pct_per_week: 0.0075`, `escalation_cap_multiplier: 3`, `weight_noise_scale_kg: 1`, `min_trend_span_days: 7`, `release_chain_max_gap_days: 14`, `weigh_in_target_interval_days: 3` (7° campo emerso dalla review: era un `/3` hard-coded nella confidenza — estensione dichiarata); (2) **attivazione `is_active=true`** — è l'interruttore: prima la fn risponde `503 config_missing` (il service_role NON bypassa il flag); (3) deploy `compute-nutrition-target` + REdeploy `analyze-meal-photo` (secret `SAFETY_NET_COACH_ID` gia' previsto dalla fetta allenamento-autonomo); (4) smoke: no header→401, JWT utente→403, service key su atleta di test→contratto (`released|gate|no_baseline_data|conflict`); (5) SOLO DOPO armare lo **scheduling settimanale** (pg_cron/esterno — fuori scope della fetta); (6) `get_advisors(security)` post-deploy. Acceptance E2E su **branch Supabase** (ledger immutabile, mai prod).
- **Decisioni registrate (OK piano 2026-07-17)**: invocazione `{athlete_id?}` singolo/batch · cattura-sicurezza = `has_pain` ultimo `daily_readiness` (weekly_checkins senza campi safety → non valutato, dichiarato nel `safety_context`) · soglie mancanti = **config v2 prima** (parse STRICT fail-loud, zero numeri nel codice) · regen types in-fetta · finestra che termina a IERI · media geometrica confidenza · pavimento>cap · LEA minore-stretto con bf% valido solo in finestra 21g · streak = `cut` consecutivi (gap ≤14g) · esenzione escalation al cambio strategia · pesi SOLO da `body_measurements` (`daily_readiness.body_weight` ignorato — debito nominato) · consenso mancante senza alert · `confidence_score` log = 1-100 · ingredienti non persistiti.
- **Debiti nominati (fetta nutrition, non fix)**: (a) release+plan+audit non transazionali (stessa famiglia del gemello); (b) nessuna fonte dati reale per le kcal-esercizio (il 300 e' una stima config-driven — quando esisterà una fonte, sostituire il parametro, non il codice); (c) `weekly_checkins` senza campi di sicurezza consumabili; (d) UI atleta/coach per i rilasci nutrizionali = fetta futura (oggi solo ledger + risposta fn); (e) rate-limit invocazioni manuali + rate-limit/quota su `analyze-meal-photo` (famiglia gia' flaggata); (f) `nutrition_logs.confidence_score` senza CHECK di range 1-100 (DDL correttivo = corsia Cowork; lo specchio resta verbatim).
- **Review avversariale di fine fetta ESEGUITA (2026-07-17, 47 agenti: 5 lenti + 2 refuter/finding + mutation-testing reale — 10/10 mutazioni uccise)**: 9 difetti distinti confermati e CHIUSI in-branch, tra cui 2 major (re-cold-start su catena viva che bypassava cap/escalation quando il coach disattiva il piano → ora cold start = piano assente E catena vuota, con catena viva guida l'ultimo rilascio; seed-peso stantio che comprimeva mesi di calo nella finestra → il seed porta la sua età reale). Dettaglio nella RETRO di `docs/auto-miglioramento.md`.
- **Fetta unpark-ai-consent (branch `claude/unpark-ai-consent`, 2026-07-16, da mergiare — ⚠ GATE LEGALE G9)** — chiude il BLOCCO GO-LIVE autonomo: `CONSENT_TYPES` **6→7** (`ai_processing` opt-in **in coda**, mai in `REQUIRED_CONSENTS`; rifiutarlo non blocca l'intake, blocca solo il rilascio autonomo — invariato), `CONSENT_VERSION` **hub-v1→hub-v2**. ⚠ **Il testo `ai_processing` è BOZZA in attesa di validazione legale (G9)**: merge/deploy SOLO dopo l'OK dell'avvocato (Nick). Prompt in-app per l'atleta già onboardato: `ConsentPromptDialog` + wiring in `AthleteTraining` (card mirror azionabile quando manca SOLO `ai_processing` + ramo `consentRequired` post-invoke; grant → retry automatico; rifiuto → spiegazione, nessun vicolo cieco). Scrittura server-authoritative: **migration `20260716170000_record_consent_rpc.sql` DA APPLICARE** (RPC `record_consent(p_type)` SECURITY DEFINER: `athlete_id=auth.uid()`, whitelist solo `ai_processing`, `granted=true` fisso, version `hub-v2` + source `in_app` pinnate server-side, insert `consents`+`audit_log`; **+ DROP `consents_insert_own`** — verificato: zero insert/upsert client su `consents` in `src/`, solo la SELECT del mirror). Test: 54 Deno + 57 vitest + tsc verdi. Review avversariale eseguita (44 agenti, 4 lenti + 2 refuter/finding, 12/20 confermati al netto dei duplicati): 5 fix chiusi in-branch (`b84f251` — cancel inerte a RPC in volo, pin letterale hub-v2, predicato condiviso mirror↔post-invoke, dialog mai orfano via withPrompt, copy PGRST202).
- **⚠ DEPLOY-INSIEME (ordine vincolante, tutto dallo stesso merge)**: (1) Cowork applica `20260716170000_record_consent_rpc.sql`; (2) deploy edge `submit-intake`; (3) publish FE. `validateConsents` è a **lunghezza esatta**: bundle FE vecchio ↔ backend nuovo (o inverso) = `invalid_payload` sull'intake finché la tab non viene ricaricata (il draft localStorage è preservato). **`release-autonomous-program` NON va rideployata** (intoccata). POST-apply: regen `types.ts` in sessione Code (rimuove i cast `as never` su `record_consent` in `useProgramRelease.ts`). Acceptance E2E (Cowork, su **branch Supabase**): `record_consent('ai_processing')` → 1 riga `consents` (granted, hub-v2, in_app) + 1 riga `audit_log` `consent_granted`; dopo il grant la release NON torna più `consentRequired`; `record_consent('health_required')` → `{ok:false, error:'invalid_consent_type'}`, zero righe.
- **Per il track legale G9 (Nick/avvocato)**: il testo bozza promette la revoca «in qualsiasi momento», ma il percorso di revoca NON esiste (RPC grant-only by design; decisione art. 7(3) rimandata — per ora revoca via coach/Cowork manuale). Da riconciliare testo o feature alla validazione. Debiti nominati: `record_consent` senza pre-check "già concesso" (replay gonfia i due ledger append-only — famiglia rate-limit già flaggata) · commento stale `release/consents.ts` r.9 («still parked») da correggere nella prossima fetta autorizzata a toccare il file (chip flaggata).
- **Fetta audit-consent-blocked (branch `claude/audit-consent-blocked`, 2026-07-15, da mergiare)** — hardening art. 22 dall'E2E reale sess.69: il ramo consenso-mancante di `release-autonomous-program` ora scrive una riga `audit_log` con `action='autonomous_release_consent_blocked'` e `metadata.missing` (lista del gate) PRIMA del `200 {consentRequired:true}` — era l'unico esito senza traccia forense. **Policy d'errore: best-effort dichiarato (decisione Nick 2026-07-15)**: insert fallito = `console.error` + risposta invariata, MAI 500 — su questa riga non viaggia alcuna escalation e un singhiozzo d'audit non deve nascondere il prompt di consenso (diverso dagli STOP, dove l'audit è fail-loud). **NIENTE `coach_alerts`**: il consenso lo risolve l'atleta all'intake, non il coach. Riga costruita dal builder puro **testato** `buildConsentBlockedAudit` (`release/decide.ts`, colonne reali, insert as-is) — asimmetria dichiarata: gli altri 2 audit della fn (STOP/release) restano inline non testati. Il debito (c) flood si estende a questo ramo: col park di `ai_processing` ogni tentativo reale scrive una riga (legata al tap dell'atleta, non automatica). **DA RIDEPLOYARE `release-autonomous-program` post-merge** (Cowork, benestare Nick); acceptance E2E su **branch Supabase**, mai prod (ledger `program_releases` immutabile).
- **Fetta allenamento-autonomo (branch `claude/allenamento-autonomo`, 2026-07-15, da mergiare)** — RILASCIO AUTONOMO del primo programma. Nuova edge fn **`release-autonomous-program`** (`verify_jwt=true` in `config.toml`, **DA DEPLOYARE**): precondizione autonoma → consenso art. 22 (lista a 3: `health_required` + `non_medical_disclaimer` + **`ai_processing`**) → gate clinico COMPLETO (safetyGate campi vivi + semaforo con **giallo = STOP** + zona general + settimana-vuota) → su STOP escalation garantita (`coach_alerts`, destinatario mai null) + `audit_log`; su pass riga IMMUTABILE in `program_releases` (schema_version 1, `config_version` 'v1' nominale — wiring `method_config` = debito dichiarato) + audit. **Motore-metodo ESTRATTO in `supabase/functions/_shared/method/`** (move meccanico provato da characterization test byte-identico; in `generate-program` cambiano SOLO 3 import). Vista atleta: `AthleteTraining` legge il rilascio reale (mock `TODAY_WORKOUT` RIMOSSO) con stati intake/consenso/revisione/genera/attesa-coach + card errore con retry. `types.ts` rigenerato (diff = solo `program_releases`). Test: 50 Deno motore + 35 Deno release + 54 Deno intake + 54 vitest (parità FE↔builder e mirror↔gate), tutti verdi + build prod verde.
- **Review avversariale di fine fetta ESEGUITA** (41 agenti, 5 lenti + refuter): 8 finding distinti confermati, 5 CHIUSI in-branch (`07f26ab` consumer zones.test dimenticato dal move — la suite intake non compilava; `208f398` blocco conservativo non può mai AUMENTARE i set + pin anti-mutazione; `b76811b` dedupe escalation: niente flood di alert high finché un `autonomous_gate_stop` è non-dismissato; `ff73828` mirror FE allineato al gate — la clearance riapre anche in UI — + gestione `isError`). **Debiti nominati (non fix)**: (a) release+audit non transazionali — se l'insert audit fallisce dopo il release la fn risponde 500 con release già scritto (RPC transazionale = fetta futura); (b) `equipment.items` non riconosciuti dal vocabolario = "palestra piena" (comportamento identico al Coached, nota M2) — in autonomo merita la whitelist dei 28 nella UI intake (fetta intake); (c) flood teorico di `audit_log` su tentativi ripetuti (rate-limit = fetta nota, già flaggata per submit-intake).
- **✅ BLOCCO GO-LIVE autonomo CHIUSO (2026-07-16, fetta unpark-ai-consent — v. bullet in cima)**. Storico: `ai_processing` esisteva nell'enum DB ma era "parked" nella raccolta intake (`validateSpec.ts CONSENT_TYPES` = 6); ogni atleta reale riceveva `{consentRequired:true, missing:['ai_processing']}` — comportamento art. 22 corretto. Resta il gate legale G9 sul testo (bozza) prima di merge/deploy.
- **Dipendenze deploy (Cowork, benestare Nick)**: (1) secret **`SAFETY_NET_COACH_ID`** sulle env della fn — fallback escalation E owner-libreria per atleti senza coach; senza, la fn fallisce forte (mai successo silenzioso); (2) deploy `release-autonomous-program`; (3) **REdeploy `generate-program`** (ora importa da `_shared/method/`) + smoke-test coach post-deploy (deployed ≠ merged); (4) seed consenso `ai_processing` per l'atleta di test.
- **Decisioni v1 registrate** (dettaglio nei commit + `objectiveDriver.ts`/`buildRelease.ts`): mappa objective→driver dichiarata in UN punto (resistenza/generale→ipertrofia, default sicuro); readiness `confidence ≤ 3` → primo blocco conservativo (−1 set, pavimento 2, mai carico in più; `importance` mai driver); il raccordo FK `workout_logs`→rilascio (ID stabili `s{n}`/`s{n}-e{m}` già nel documento) = coda immediata dell'anello.

### Addendum 2026-07-14 (storico — superato dove diverge)

- **Fetta intake-form-fase2 (branch `claude/intake-form-fase2`, 2026-07-14, da mergiare)** — Fase 2 del questionario: form config-driven in `src/features/intake/**` (CORE + strato Autonoma con neurotipo 30-Q in 5 pagine), submit SOLO via edge fn `submit-intake`, esiti reali renderizzati (routedOut prioritario su ok), rotta `/onboarding` swappata e **wizard legacy RITIRATO** (via la `profiles.update` client-side + `analyzeOnboarding` + set 15-Q). 39 test vitest verdi (parità col validatore server importato nei test) + `tsc` + build prod verdi. Review avversariale di fine fetta (4 lenti + verificatori): 4 major confermati e CHIUSI in-branch (`12a0ecf` — draft art. 9 user-scoped e pulito al signOut, niente wipe su authError, pin integrale ordine neurotipo, mapping esiti testato). Decisioni/deviazioni: `docs/prompts/2026-07-14-intake-form-fase2.md` (Addendum). **Testi-consensi `hub-v1` APPROVATI da Nick il 2026-07-14** (medical_sharing riformulato da lui in `2bb5c23`; annotazione definitiva in `labels.ts`/`ConsentsStep.tsx`). L'Informativa Privacy (`PrivacyPolicy.tsx`: titolare, contatti, sezione art. 9) è un **track legale separato di Nick**, fuori scope dalle fette Code.
- **Stato DB verificato 2026-07-14 (`list_migrations` via connettore):** le 3 migration intake `20260713150000..150002` E l'hardening `20260714101458_harden_profile_escalation_mode_tier` risultano **APPLICATE live**; `submit-intake` deployata. **types.ts RIGENERATO** sul branch (commit `c40ebc5`, diff solo-additivo) + `Profile` esteso (`coaching_mode`/`tier`/`objective`).
- **⚠ DEBITO GO-LIVE ora SBLOCCATO**: col ritiro del write client-side del wizard, Cowork (benestare Nick) può estendere l'hardening a `medical_clearance_required`/`red_flags`/`fms_exclusion_zones` — da fare DOPO il merge di questa fetta (il wizard legacy che le scriveva non esiste più sul branch).
- **E2E submit reale**: nessun atleta di test disponibile — Nick lo chiude sul **primo invito reale** (decisione 2026-07-14, non bloccante per il merge).

### Addendum 2026-07-13 (storico — superato dove diverge)

- **Fetta intake-redesign (branch `claude/intake-redesign`, 2026-07-13, NON mergiata)** — backbone Fase 1 del questionario config-driven: 3 **migration-gemelle DA APPLICARE** (Cowork via connettore, benestare Nick) `20260713150000_intake_objective_cycle` · `20260713150001_intake_handle_new_user_mode_tier` · `20260713150002_intake_submit_rpc` (RPC privata service-role); edge fn **NUOVA `submit-intake`** (DA DEPLOYARE dopo l'apply) + **`invite-athlete` estesa** coaching_mode/tier (DA RIDEPLOYARE); dialog invito con selettori Modalità/Piano; **54 test Deno verdi** (`npx deno test --no-lock supabase/functions/submit-intake/intake/`). Decisioni e deviazioni approvate all'OK: `docs/prompts/2026-07-13-intake-redesign.md` (Addendum esecuzione). **Fase 2** (fetta successiva): rendering config-driven del form, 30-Q nella UI, selettore zone/infortuni, rewire `OnboardingWizard` su `submit-intake`, ritiro `nc-questionnaire` + flusso `invite_tokens`.
- **types.ts regen PENDENTE (esteso)**: rigenerare SOLO dopo l'apply delle 3 migration intake — un solo regen copre anche F0 (già applicata live, `20260712150000..150005`) — poi build gate.
- **⚠ DEBITO GO-LIVE (gate di Fase 2, PRIMA di clienti reali)**: estendere `prevent_profile_privilege_escalation` a `coaching_mode`/`tier`/`medical_clearance_required`/`red_flags` — oggi un atleta può riscriverli via UPDATE own-row ("Users can update their own profile" + trigger che protegge solo role/coach_id/subscription\_\*). L'hardening va sequenziato DOPO il rewire del wizard su `submit_intake`: il wizard legacy scrive quelle colonne client-side e si romperebbe.
- **Motore-metodo M1/M2 in `main`**: `generate-program` è deterministica (5 strati, zero AI) con **45 test Deno verdi** — `deno test --cached-only supabase/functions/generate-program/method/`.
- **Invito nativo FATTO e primario**: la UI invoca la edge fn `invite-athlete` (`src/components/coach/InviteAthleteDialog.tsx:159`).
- **Libreria esercizi POPOLATA**: `exercises` = **954 righe, tutte con `coach_id` valorizzato** (verifica Cowork via connettore, 2026-07-12). La condizione SENTINEL di §4 r.8 non è più lo stato normale.
- **Branch vivi non mergiati**: `claude/zonemap-v18` (**zoneMap v18**: split dorsale/toracica + clamp per-zona `ZONE_BASE` + conform-delta FASE B; **49/49 test Deno**; **DEPLOYATA 2026-07-13**: edge fn `generate-program` **version 18 ACTIVE, `verify_jwt=true`** via connettore, al "procedi" esplicito di Nick; resta il **merge** — prompt-file `docs/prompts/2026-07-12-zonemap-v18.md`; push del branch da GitHub Desktop, il classifier ha bloccato il push dalla corsia Code) · `claude/invite-resend-hardening` (+4 — hardening reinvio invito).
- **`claude/zonemap-riconc` SUPERSEDED (2026-07-13)**: bozza Delta 1 divergente dalla spec riconciliata (`mid back`→toracica, alias extra `lat`/`gran dorsale`, senza Delta D) — **eliminare senza merge** (locale + `origin/claude/zonemap-riconc`, decisione Nick): un merge post-v18 regredirebbe la spec.
- **Debito noto (registrato, NON risolto in questa fetta)**: 1 migration correttiva out-of-band applicata sul DB senza file nel repo — DB = **129 versioni**, repo = **128 file**.
- **Fotografia completa del repo**: `docs/stato-repo-2026-07-12.md` (ricognizione read-only, 2026-07-12).

## 1. In una riga

Migrazione di **nc-performance-hub** (coaching dual-interface: Coach "Aura" web + Atleta PWA) da **Lovable Cloud** a **Supabase di proprietà** (ref `xgxtplqlewpqjzghvbke`). AI→OpenAI **validata 6/6**, login Google **attivo**, invito→atleta **verificato**, fix roster + **`generate-program` cablata**. Restano: collegare i moduli WIP e il cutover finale del `.env` (la libreria esercizi è stata **popolata** — 954 righe, v. Addendum §0).

## 2. Stato attuale (fattuale)

- **Supabase di proprietà.** Ref **`xgxtplqlewpqjzghvbke`**, region `eu-central-1`, org `umydelvpdzieopddfhpf`. URL `https://xgxtplqlewpqjzghvbke.supabase.co` · publishable key `sb_publishable_Hr-lQDDqFZZXZgfjLK999A_JkCfRg1f`. 54 tabelle, **RLS attiva ovunque**, advisor security 0 ERROR.
- **6 funzioni AI su OpenAI, validate live:** ✅ `ask-copilot`, `analyze-meal-photo`, `chat-with-coach`, `generate-batch-checkins` (2026-06-14) · ✅ `generate-program` (gpt-5.2) e `analyze-athlete-week` (gpt-5.4-mini) (2026-06-15, atleta reale `wolfwood370` id `912d6214`). `OPENAI_API_KEY` con credito; secret OK (OpenAI/Resend/Stripe test).
- **Login Google attivo.** Coach = `nctrainingsystems@gmail.com` (id `af93b1cd`). **Atleti = su invito.** Meccanismo verificato: la **UI invito scrive `invite_tokens`**; al **signup Google** il trigger `handle_new_user` collega via path `invite_tokens` (email) → `profiles.coach_id` + `used=true`. NB profilo prende il nome dall'account Google. (Aggiornamento 2026-07-05: la edge fn `invite-athlete` è ora il **path primario, invocato dalla UI** — `InviteAthleteDialog.tsx:159`.)
- **Git: `origin/main` (tip `2296074`).** Mergiati: migrazione, audit, E2E nativa, rebranding, guard coach, **fix cache roster** (`2a57af1`), **generate-program UI** (`8735e0d`+`afb9e1e`), **D12 Set A** (`6dcd2fc`), **track/recupero doc** (`f6bd4b2`/`1de3b7f`), **dual-agent `COWORK.md`** (`69e8a4a`), e l'intera **campagna autonoma D13** (Epic A + C1–C5 + D E2E + B audit-report). Working tree pulito.
- **Campagna autonoma D13 COMPLETA (2026-06-18).** 7 hook WIP collegati alla UI coach: **Quota AI** (`AiQuotaBadge` in Master Copilot + AI Brain), **Gating** (`FeatureGate` su video/AI/`max_active_programs`), **Nutrizione** (`NutritionAdherenceCard` nella tab Strategia), **FMS/Salute** (tab Salute `HealthProfileTab` + `FmsContraindicationBadge` nel Program Builder), **Periodizzazione** (`PeriodizationTab` CRUD + `TrainingBlocksTimeline`). **Suite E2E** Playwright verde (**30 pass / 17 skip** senza credenziali). **Audit pulito** — nessun nuovo dead-code, report annotato in `docs/D13_AUDIT_REPORT.md`. Ogni Epic verificato (tsc + `audit:all` + review avversariale multi-agente). Spec/piano: `docs/D13_AUTONOMOUS_CAMPAIGN.md`.
- **Libreria esercizi POPOLATA (2026-07-12)**: `exercises` = **954 righe con `coach_id`** (verifica Cowork via connettore). Storico: era vuota fino a luglio (nessun seed nelle migration — i dati esistevano solo nel vecchio DB Lovable). FE puntato al backend nuovo via `.env.local` (**TEMP**, non è il cutover D6).
- **Qualità:** audit (solo `ai-error` rimosso, WIP tenuti in `docs/WIP_MODULES.md`) · E2E nativa 6/6 · rebranding "NC Performance Hub" · `ProtectedCoachRoute` su 15 route · fix roster `src/lib/coachQueries.ts` · `generate-program` cablata (`GenerateAiWeekDialog` + `useGenerateProgram` + `mapAiDaysToSessions` + store `replaceWeekWithAiProgram`) · **D12 Set A** dead-code ACWR rimosso (`6dcd2fc`, `tsc` verde + audit pulito).
- **Lovable resta intatto** → rollback a costo ~zero fino al cutover.

## 3. Decisioni

**Chiuse:** D1 schema-only · D2 AI→OpenAI (validata 6/6) · D3 OAuth Google · audit · E2E · rebranding · guard coach · smoke AI + invito (2026-06-15) · fix cache roster (2026-06-15) · **generate-program UI (D11, 2026-06-16)** · **D12 Set A cleanup ACWR (2026-06-17, `6dcd2fc`)** · **dual-agent Code/Cowork formalizzato (`COWORK.md`, 2026-06-18, `69e8a4a`)** · **campagna autonoma D13 COMPLETA (2026-06-18)** — 7 hook WIP collegati + suite E2E verde + audit pulito (`docs/D13_AUDIT_REPORT.md`) · **✅ D5 RISOLTA (2026-07-04, metodo v2):** security = ownership condivisa (5 attori) — Code = codice sicuro + `/security-review`; Cowork = advisors/RLS/DB via connettore col benestare di Nick. Vedi `CLAUDE.md` legge #11 / `COWORK.md §5` / `03-BACKEND-SUPABASE.md §0`.
**Aperte:** **D4** hosting FE (Cloudflare Pages vs Vercel) · **D6** timing cutover `.env` · **libreria esercizi** (come popolarla, §4 r.8) · **D12 Set B** (`readinessMath`/`constants`) **tenuto** — rimuovere solo su conferma esplicita di abbandono della feature readiness.

## 4. Prossimi passi

| #   | Passo                                                                                                                                                                                                                                                                                                               | Owner             |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------- |
| 1   | ✅ **FATTO** Smoke AI 6/6 con atleta reale.                                                                                                                                                                                                                                                                         | 👤 + 🤝           |
| 2   | ✅ **FATTO** Flusso invito→atleta verificato.                                                                                                                                                                                                                                                                       | 👤 + 🤝           |
| 3   | ✅ **FATTO 2026-06-18 (campagna D13)** Collegati 7 hook WIP coach (Quota AI, Gating, Nutrizione, FMS/Salute, Periodizzazione) + suite E2E + audit report. Restano WIP solo moduli athlete-side/infra (offline/PWA, ciclo mestruale, media, foodApi) — `docs/WIP_MODULES.md`.                                        | Claude Code       |
| 4   | **Cutover finale** `.env`/backend (D6/D4) + scollegare Lovable. (Ora override locale `.env.local` → backend nuovo.)                                                                                                                                                                                                 | 👤                |
| 5   | Minori: hook husky, email/handle legacy, tidy RLS `{public}`→`{authenticated}`                                                                                                                                                                                                                                      | 👤 / CC / Lovable |
| 6   | ✅ **FATTO+VALIDATO 2026-06-15** (`2a57af1`) Fix cache lista atleti: `coachQueries.ts` `COACH_ROSTER_QUERY_OPTS` su 7 letture roster. `/coach/athletes` mostra l'atleta.                                                                                                                                            | Claude Code       |
| 7   | ✅ **FATTO 2026-06-16** (`8735e0d` feat + `afb9e1e` cleanup) `generate-program` cablata: `GenerateAiWeekDialog` (header ProgramBuilder) → hook → mapper → store. Fallback **SENTINEL** `UNLINKED_EXERCISE_ID` finché libreria vuota. Piano: `docs/D11_GENERATE_PROGRAM_PLAN.md`.                                    | Claude Code       |
| 8   | ✅ **SUPERATO 2026-07-12** — libreria popolata: `exercises` = **954 righe, tutte con `coach_id`** (verifica Cowork via connettore). Storico: era vuota (0 righe) e bloccava il program-building reale (SENTINEL).                                                                                                   | 👤                |
| 9   | ✅ **FATTO 2026-06-17** (`6dcd2fc`) **D12 Set A** — rimossi `useAcwrData`+`trainingMetrics` (duplicato ACWR morto), `tsc` verde + audit pulito. Set B (`readinessMath`/`constants`) **tenuto** (roadmap). Piano: `docs/D12_DEADCODE_ACWR_CLEANUP_PLAN.md`.                                                          | Claude Code       |
| 10  | **`.env` + stash GHD** — estrai/metti al sicuro il `.env` dallo stash `oauth-migration`, **poi droppa** i 3 stash residui (`coach-guard`, `e2e-native`, `oauth-migration`). I doc unici sono già recuperati e trackati (`1de3b7f`); negli stash restano solo `.env` (secret) + artefatti Playwright (rigenerabili). | 👤 + CC           |
| 11  | **Credenziali E2E** — impostare `E2E_COACH_EMAIL`/`E2E_COACH_PASSWORD` (coach `test@test.com`, D3 §6) per attivare i **17 test autenticati** skippati della suite Playwright (project `chromium-coach`).                                                                                                            | 👤                |
| 12  | **D12 Set B** (`readinessMath`/`constants`) — decisione: **tieni** (default, roadmap readiness) o rimuovi se la feature è abbandonata. Rimozione SOLO su conferma esplicita → STOP&ASK.                                                                                                                             | 👤                |
| 13  | **Token `--warning` marrone in light** — in `forced-light` (`App.tsx`) `--warning` è `#774616` (tertiary-container) → `text-warning` rende marrone non ambra. Riguarda `AcwrGauge` + card D13. Micro-task cosmetico cross-cutting (decisione design).                                                               | 👤 / CC           |

## 5. Tooling & workflow

- **Si alternano** Cowork (infra/connettore/test Chrome/doc) e **Claude Code** (codice/branch/commit). Stesso checkout.
- **Branch hygiene (CC):** non committare su `main`; usare `claude/<slug>`.
- **Push/merge:** li fa **Nick via GitHub Desktop** (MAI Cowork/CC).
- **✅ Stash-loop GHD risolto (2026-06-17):** prima, a ogni branch switch GHD stashava le modifiche non committate **inclusi i file untracked** → i doc "sparivano" (accumulati 6 stash `!!GitHub_Desktop<branch>`). **Fix applicato:** tutti i doc di riferimento sono ora **trackati** (`f6bd4b2` HANDOFF+D11/D12; `1de3b7f` recupero D2/D3/D7/D8/D9/D10/PRODUCT_SPEC/ROADMAP/CLAUDE_CODE_SETUP/DB_MIGRATION\*/SECRETS_SETUP dagli stash). **Restano 3 stash** (`coach-guard`, `e2e-native`, `oauth-migration`) con ancora `.env` (secret) + artefatti Playwright: **droppare SOLO dopo** che Nick ha messo al sicuro `.env` (vedi §4 r.10). Lezione: i doc vanno committati, mai lasciati untracked.
- **Cowork sandbox:** file montati con NUL padding → usa `Read`/`grep -a`; `git status` mostra tutto il tree modificato (CRLF, falso) e può lasciare `index.lock` → git read-only in Cowork; `knip`/`depcheck` in CC.
- Anteprima/dev = `npm run dev` (`localhost:8080`).

## 5-bis. Registro attori ↔ repo

- `main` = fonte di verità · **un attore alla volta sul working tree**. Cowork aggiorna questa tabella a inizio/fine sessione.

| Repo                                                                   | Attore attivo ora               | Corsia                              |
| ---------------------------------------------------------------------- | ------------------------------- | ----------------------------------- |
| nc-performance-hub                                                     | — (compilare a inizio sessione) | build modulo / DB / handoff         |
| satelliti (questionnaire · calendar · movement · business · education) | read-only per Cowork            | cave da fondere nel Hub, non attivi |

## 6. Guardrail (vincolanti)

Risposte/commit **italiano** · **MAI push** (sincronizza Nick) · build gate `tsc --noEmit -p tsconfig.app.json` verde · commit atomici · secrets/credenziali/Stripe/Google = Nick · security = ownership condivisa (legge #11): Code = codice sicuro + `/security-review` ai milestone; il DB (RLS/advisor/migration) lo opera Cowork via connettore col benestare di Nick · niente operazioni distruttive senza conferma · confermare costo prima di creare risorse Supabase.

## 7. Documenti di riferimento

Tutti **trackati** (2026-06-18): `CLAUDE.md` · `COWORK.md` + `.claude/methodology/*` · `docs/D2_OPENAI_MIGRATION_CONTRACT.md` · `docs/D3_TEST_200_RUNBOOK.md` · `docs/D7_GOOGLE_OAUTH_SETUP.md` · `docs/D8_AUDIT_CODICE_MORTO.md` · `docs/D9_E2E_PLAYWRIGHT_NATIVE.md` · `docs/D10_COACH_ROUTE_GUARD.md` · `docs/D11_GENERATE_PROGRAM_PLAN.md` · `docs/D12_DEADCODE_ACWR_CLEANUP_PLAN.md` · `docs/WIP_MODULES.md` · `docs/CLAUDE_CODE_SETUP.md` · `docs/DB_MIGRATION.md` · `docs/DB_MIGRATION_PREFLIGHT.md` · `docs/DB_MIGRATION_FASE1_REPORT.md` · `docs/SECRETS_SETUP.md` · `docs/PRODUCT_SPEC.md` · `docs/ROADMAP.md` · `docs/UX_UI_DESIGN_SYSTEM.md` · `docs/D13_AUTONOMOUS_CAMPAIGN.md` · `docs/D13_AUDIT_REPORT.md`.

---

## 8. PROMPT DI TRASFERIMENTO — Claude Code (lavoro su CODICE)

```
Prosecuzione nc-performance-hub (Lovable → Supabase mio). Leggi PRIMA docs/HANDOFF.md
+ CLAUDE.md + docs/auto-miglioramento.md + .claude/methodology/00-CORE.md.

Stato: vedi Addendum §0 (stato vivo, prevale) + §2 per lo storico (campagna D13 completa —
dettagli in docs/D13_AUDIT_REPORT.md). Libreria esercizi POPOLATA (954 righe); branch vivi
non mergiati: claude/zonemap-riconc (+1) e claude/invite-resend-hardening (+4).

GUARDRAIL: italiano; all'inizio VERIFICA il branch e crea claude/<slug> (NON su main); MAI push
(sincronizzo io); build gate tsc --noEmit -p tsconfig.app.json verde; commit atomici; secrets le
imposto io; security = ownership condivisa (legge #11): tu = codice sicuro + /security-review,
il DB lo tocca Cowork col benestare di Nick — tu proponi il FILE di migration. MONITORA il
contesto: a ~85% fermati, dichiaralo e prepara handoff + prompt di ripartenza.

OBIETTIVO (dimmi tu quale): collegare un modulo WIP da docs/WIP_MODULES.md, o un fix FE.
Esplora→pianifica e proponi il piano PRIMA di modificare.
```

## 9. PROMPT DI TRASFERIMENTO — Cowork (lavoro su CONNETTORE/INFRA)

```
Prosecuzione nc-performance-hub su Cowork. Leggi PRIMA COWORK.md (corsia Cowork) + docs/HANDOFF.md
+ docs/auto-miglioramento.md.

Stato: vedi Addendum §0 (stato vivo, prevale) + §2 per lo storico (dual-agent attivo; campagna D13
completa — dettagli in docs/D13_AUDIT_REPORT.md). Backend ref xgxtplqlewpqjzghvbke; LIBRERIA
ESERCIZI POPOLATA (954 righe con coach_id, verifica 2026-07-12); FE su .env.local (TEMP). Resume
del progetto Supabase (free-tier pausing) prima di ogni lavoro DB.

CORSIA COWORK (da COWORK.md): git READ-ONLY; niente scritture nel repo (i file li committa Code su
branch); il DB è il tuo binario: apply_migration (DDL) + il FILE supabase/migrations/* lo committa
Code + get_advisors dopo ogni DDL; execute_sql per DML/seed; backup prima dei distruttivi; deviazione
MCP-su-prod dichiarata, ok finché zero dati reali. Verifica prima di ogni distruttivo (stash/.env);
secrets/.env/Stripe/Google = Nick; security = advisors/RLS/review DB via connettore col benestare di
Nick; MAI push; esplora→pianifica→proponi PRIMA di agire; a ~85% di contesto fermati e prepara
handoff + prompt.

OBIETTIVO (dimmi tu quale): (a) QA della libreria esercizi popolata (954 righe: qualità dati /
campi metodo); (b) assistere estrazione .env + cleanup dei 3 stash GHD residui;
(c) cutover .env/backend (D6/D4) + scollegare Lovable; (d) verifica dati/connettore sulle nuove UI D13.
Proponimi il micro-piano e procedi.
```

---

_Hand-off aggiornato 2026-06-18 (campagna autonoma **D13 COMPLETA** su `origin/main` tip `2296074`: 7 hook WIP collegati + E2E verde + audit pulito `docs/D13_AUDIT_REPORT.md`; D12 Set A `6dcd2fc`, Set B tenuto; dual-agent `COWORK.md` `69e8a4a`; stash-loop chiuso `f6bd4b2`/`1de3b7f`). (2026-07-04) **metodo v2**: file-guida allineati a Supabase proprio + 5 attori; D5 risolta; auto-miglioramento + registro attori↔repo aggiunti. Aggiornare §2/§3/§4 man mano._

_(2026-07-12) **Fetta riordino** (fotografia: `docs/stato-repo-2026-07-12.md`): `.env` fuori dal tracking + `.env.example` coi soli nomi · lockfile unico npm · `engines` node ≥24 · CLAUDE.md a verità · banner STORICO su D2/D7/D9/D10/D11 + nota rail su D13 · cast `as any` su `program_blocks` rimosso · branch mergiati eliminati + worktree orfano rimosso. Stato vivo in **Addendum §0**: exercises = 954 righe · debito 1 migration out-of-band (129 DB vs 128 repo) · branch vivi zonemap-riconc / invite-resend-hardening. Aperti residui (§4): cutover D6 `.env`+stash, credenziali E2E, Set B, token --warning._
